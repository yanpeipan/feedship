---
phase: quick
plan: "260328-4l9"
subsystem: cli
tags: [refactor, cli, search, print_articles]
dependency_graph:
  requires: []
  provides:
    - "src/application/search.py: print_articles function"
  affects:
    - "src/cli/article.py: article_list, article_search commands"
tech_stack:
  added:
    - "print_articles(items, mode, verbose) - unified CLI output function"
    - "_print_article_verbose(item, mode) - verbose output helper"
  patterns:
    - "click.secho for all console output (no Rich dependencies in print_articles)"
    - "mode parameter determines header format and verbose field selection"
key_files:
  created: []
  modified:
    - "src/application/search.py: added print_articles and _print_article_verbose functions"
    - "src/cli/article.py: refactored article_list and article_search to use print_articles"
decisions:
  - "Used mode parameter ('list', 'semantic', 'fts') to differentiate header formats and verbose field selection"
  - "Imported click locally in print_articles to avoid circular imports"
  - "Semantic search gracefully handles torch import failure with proper error message"
metrics:
  duration: "~5 minutes"
  completed: "2026-03-28"
---

# Phase quick Plan 260328-4l9: Add print_articles Function Summary

## One-liner

Unified print_articles() function replaces Rich Table and click.secho loops across article list and search CLI commands.

## Objective

Refactor article CLI commands to use unified print_articles function. All three commands (list, search semantic, search fts) should call rank_*_results() to get scored data, format_*() to get formatted data, then print_articles() to display. No inline click.secho printing in CLI.

## Completed Tasks

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Add print_articles function to search.py | 3cda776 | src/application/search.py |
| 2 | Refactor article_list command | 3cda776 | src/cli/article.py |
| 3 | Refactor article_search command | 3cda776 | src/cli/article.py |

## Changes Made

### src/application/search.py
Added `print_articles(items, mode, verbose)` and `_print_article_verbose(item, mode)` functions that:
- Accept formatted article list, mode ('list', 'semantic', 'fts'), and verbose flag
- Print mode-specific headers with proper column labels
- Use click.secho for all output (no Rich dependencies)
- Handle verbose output with mode-specific field selection

### src/cli/article.py
- **article_list**: Replaced Rich Table creation with single `print_articles(formatted, 'list', verbose=verbose)` call
- **article_search (semantic)**: Replaced 8-line click.secho loop with `print_articles(formatted, 'semantic', verbose=verbose)`
- **article_search (fts)**: Replaced 6-line click.secho loop with `print_articles(formatted, 'fts', verbose=verbose)`
- Removed unused Console and Table imports from Rich

## Verification Results

| Command | Status |
|---------|--------|
| `python -m src.cli article list --limit 5` | PASS - formatted table output |
| `python -m src.cli search "test"` | PASS - FTS results with formatted output |
| `python -m src.cli article list --limit 2 --verbose` | PASS - verbose output with full fields |
| `python -m src.cli search "test" --semantic` | EXPECTED FAIL - torch not installed, error handled gracefully |

## Success Criteria

- [x] src/application/search.py exports print_articles function
- [x] article_list in src/cli/article.py calls print_articles(formatted, 'list', verbose=verbose)
- [x] article_search semantic branch calls print_articles(formatted, 'semantic', verbose=verbose)
- [x] article_search fts branch calls print_articles(formatted, 'fts', verbose=verbose)
- [x] No Rich Table creation in article_list
- [x] No click.secho printing loops in article_search
- [x] All three commands produce formatted output with unified appearance

## Deviations from Plan

None - plan executed exactly as written.

## Auth Gates

None.
