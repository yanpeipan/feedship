---
phase: quick-260328-3w1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/search.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Semantic search results have score field with normalized 0-1 value"
    - "FTS search results have score field with fixed 1.0 value"
    - "List results have score field with fixed 1.0 value"
    - "All three ranking functions share the same interface structure"
  artifacts:
    - path: "src/application/search.py"
      provides: "rank_semantic_results, rank_fts_results, rank_list_results functions"
      min_lines: 50
      exports: ["rank_semantic_results", "rank_fts_results", "rank_list_results"]
  key_links:
    - from: "src/application/search.py"
      to: "rank_semantic_results"
      via: "returns norm_similarity as score"
    - from: "src/application/search.py"
      to: "rank_fts_results"
      via: "returns fixed 1.0 as score"
    - from: "src/application/search.py"
      to: "rank_list_results"
      via: "returns fixed 1.0 as score"
---

<objective>
Refactor ranking functions to unify score output: semantic search returns normalized 0-1 score, FTS and list return fixed 1.0. Create rank_fts_results and rank_list_results as new functions with consistent interface.
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/application/search.py (current ranking functions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rank_fts_results function</name>
  <files>src/application/search.py</files>
  <action>
    Add `rank_fts_results(articles: list[ArticleListItem]) -> list[dict[str, Any]]` function.

    Input: List of ArticleListItem from search_articles
    Output: Each article dict with added `score` field = 1.0 (fixed score, no similarity metric for FTS)

    Also add `rank_list_results(items: list[ArticleListItem]) -> list[dict[str, Any]]` function with identical logic.

    Both functions should:
    - Return list of dicts with `score` field = 1.0
    - Preserve all original article fields
    - Return empty list if input is empty
  </action>
  <verify>grep -n "^def rank_fts_results\|^def rank_list_results" src/application/search.py</verify>
  <done>FTS and list ranking functions exist, each adds score=1.0 to results</done>
</task>

<task type="auto">
  <name>Task 2: Update format_fts_results to use score field</name>
  <files>src/application/search.py</files>
  <action>
    Modify `_format_fts_items` in format_articles() to use `score` field if present (from rank_fts_results), otherwise fall back to "FTS" string for backward compatibility.

    Change score logic in _format_fts_items:
    - If item has 'score' field: use it (format as percentage or "1" or "100%")
    - Else: use "FTS" string (backward compatibility)

    Similarly update _format_list_items to use 'score' field if present, otherwise use "LIST".
  </action>
  <verify>python -c "from src.application.search import format_articles; print(format_articles([{'title': 'Test', 'feed_name': 'F', 'pub_date': '2024-01-01', 'score': 1.0}], mode='fts'))"</verify>
  <done>format_fts_results and format_articles (mode=fts/list) show score from ranking function</done>
</task>

<task type="auto">
  <name>Task 3: Verify rank_semantic_results outputs score field</name>
  <files>src/application/search.py</files>
  <action>
    Verify rank_semantic_results returns `score` field (should already return norm_similarity).

    Check that format_semantic_results / _format_semantic_items uses the `score` field if present, or falls back to norm_similarity for backward compatibility.

    If format_semantic_results does not use `score`, update _format_semantic_items to check for `score` field first, then fall back to `norm_similarity`.
  </action>
  <verify>python -c "from src.application.search import rank_semantic_results; print([r.get('score') for r in rank_semantic_results([{'sqlite_id': 'test', 'distance': 0.3}], top_k=1)])"</verify>
  <done>rank_semantic_results provides score field consumed by format_semantic_results</done>
</task>

</tasks>

<verification>
python -c "
from src.application.search import rank_semantic_results, rank_fts_results, rank_list_results, format_articles
# Test semantic
sem = rank_semantic_results([{'sqlite_id': 'test', 'distance': 0.3, 'title': 'T', 'url': 'http://x.co', 'article_id': 'a1'}], top_k=1)
print('Semantic score:', sem[0].get('score') if sem else 'N/A')
# Test FTS (need ArticleListItem - just check function exists)
print('rank_fts_results exists:', 'rank_fts_results' in dir())
# Test list
print('rank_list_results exists:', 'rank_list_results' in dir())
"
</verification>

<success_criteria>
- rank_semantic_results returns score field with 0-1 normalized value
- rank_fts_results exists and returns score=1.0 for all results
- rank_list_results exists and returns score=1.0 for all results
- format_fts_results uses score field when available
- format_articles (mode=fts/list) uses score field when available
</success_criteria>

<output>
After completion, create `.planning/quick/260328-3w1-rank-semantic-results-rank-fts-results-r/260328-3w1-SUMMARY.md`
</output>
