---
phase: quick
plan: 260327-jt7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tags/ai_tagging.py
  - src/application/tags.py
  - src/storage/sqlite.py
  - src/storage/__init__.py
  - src/cli/article.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AI clustering auto-tagging code (ai_tagging.py) is removed"
    - "SQLite-vec based embedding storage is removed"
    - "article tag --auto command is removed"
    - "article tag --rules and article tag <id> <name> commands still work"
  artifacts:
    - path: "src/tags/ai_tagging.py"
      provides: "Deleted - was using obsolete SQLite-vec embeddings"
    - path: "src/storage/sqlite.py"
      provides: "Removed store_embedding, get_article_embedding, get_all_embeddings, get_articles_without_embeddings, _ensure_embeddings_table, article_embeddings table"
  key_links:
    - from: "src/cli/article.py"
      to: "src/application/tags.py"
      via: "import auto_tag_articles - REMOVED"
    - from: "src/application/tags.py"
      to: "src/tags/ai_tagging.py"
      via: "import run_auto_tagging - REMOVED"
---

<objective>
Remove dead code from the tag system: the obsolete AI clustering-based auto-tagging feature that used SQLite-vec embeddings, superseded by v1.8 ChromaDB semantic search.

Purpose: Clean up code that is no longer used since v1.8 uses ChromaDB for embeddings and semantic search.
Output: Removed files and updated code paths.
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/tags/ai_tagging.py (entire file - AI clustering with SQLite-vec embeddings)
@src/application/tags.py (auto_tag_articles wrapper)
@src/storage/sqlite.py (article_embeddings table and related functions)
@src/cli/article.py (article tag --auto command)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/tags/ai_tagging.py</name>
  <files>src/tags/ai_tagging.py</files>
  <action>
Delete the entire file src/tags/ai_tagging.py. This file contains:
- generate_embedding(), generate_embeddings_for_articles() - embedding generation using sentence-transformers
- discover_clusters(), suggest_tag_name_from_articles() - DBSCAN clustering
- run_auto_tagging() - main pipeline
- All using SQLite-vec for storage, superseded by v1.8 ChromaDB

This file is dead code because v1.8 uses ChromaDB for embeddings and semantic search instead.
  </action>
  <verify>
<automated>python -c "import src.tags.ai_tagging" 2>&1 | grep -q "No module" && echo "Module deleted successfully" || echo "ERROR: Module still exists"</automated>
  </verify>
  <done>src/tags/ai_tagging.py deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update src/application/tags.py - remove auto_tag_articles</name>
  <files>src/application/tags.py</files>
  <action>
Update src/application/tags.py to remove:
1. The import: from src.tags.ai_tagging import run_auto_tagging
2. The auto_tag_articles() function entirely

Keep the following functions as they are still used:
- apply_rules_to_untagged() - used by article tag --rules
- tag_article_manual() - used by article tag <id> <name>
</action>
  <verify>
<automated>python -c "from src.application.tags import apply_rules_to_untagged, tag_article_manual; print('Imports OK')"</automated>
  </verify>
  <done>src/application/tags.py no longer imports or references ai_tagging</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead storage functions from src/storage/sqlite.py</name>
  <files>src/storage/sqlite.py</files>
  <action>
Remove from src/storage/sqlite.py the following functions that were only used by ai_tagging.py:

1. _ensure_embeddings_table() function (lines ~395-406)
2. store_embedding() function (lines ~409-421)
3. get_article_embedding() function (lines ~424-434)
4. get_all_embeddings() function (lines ~437-451)
5. get_articles_without_embeddings() function (lines ~454-463)

Also remove the article_embeddings table creation from init_db() if it exists.
These functions used sqlite-vec extension for storing embeddings in SQLite, superseded by ChromaDB in v1.8.
</action>
  <verify>
<automated>python -c "from src.storage.sqlite import store_embedding, get_article_embedding, get_all_embeddings, get_articles_without_embeddings" 2>&1 | grep -q "cannot import" && echo "Dead functions removed" || echo "ERROR: Functions still exist"</automated>
  </verify>
  <done>SQLite embedding storage functions removed from storage layer</done>
</task>

<task type="auto">
  <name>Task 4: Update src/storage/__init__.py - remove dead exports</name>
  <files>src/storage/__init__.py</files>
  <action>
Update src/storage/__init__.py to remove exports that no longer exist:
- Remove: store_embedding
- Remove: get_article_embedding
- Remove: get_all_embeddings
- Remove: get_articles_without_embeddings

Keep other exports that are still used.
</action>
  <verify>
<automated>python -c "from src.storage import store_embedding, get_article_embedding, get_all_embeddings, get_articles_without_embeddings" 2>&1 | grep -q "cannot import" && echo "Dead exports removed" || echo "ERROR: Exports still exist"</automated>
  </verify>
  <done>src/storage/__init__.py updated, dead exports removed</done>
</task>

<task type="auto">
  <name>Task 5: Update src/cli/article.py - remove --auto option</name>
  <files>src/cli/article.py</files>
  <action>
Update src/cli/article.py article_tag command:

1. Remove the import of auto_tag_articles:
   - Change: from src.application.tags import auto_tag_articles, apply_rules_to_untagged, tag_article_manual
   - To: from src.application.tags import apply_rules_to_untagged, tag_article_manual

2. Remove the --auto option from the command decorator (line 104)

3. Remove the auto_tag_articles() function call block (lines 110-120)

4. Keep the --rules option and --auto flag removal (lines 104, 110 should be removed together)

5. Update the help text - remove reference to --auto, keep --rules

The command should now only support:
- article tag <article-id> <tag-name> (manual tagging)
- article tag --rules (apply rules to untagged)
</action>
  <verify>
<automated>python -c "from src.cli.article import article_tag; print('CLI import OK')"</automated>
  </verify>
  <done>article tag --auto removed, --rules and manual tagging preserved</done>
</task>

</tasks>

<verification>
Run: python -c "from src.application.tags import apply_rules_to_untagged, tag_article_manual; from src.cli.article import article_tag; print('All imports OK')"
</verification>

<success_criteria>
- src/tags/ai_tagging.py deleted
- src/application/tags.py no longer imports ai_tagging
- SQLite embedding storage functions removed (store_embedding, get_article_embedding, etc.)
- article tag --auto command removed
- article tag --rules still works
- article tag <id> <name> still works
- No import errors for remaining code
</success_criteria>

<output>
After completion, create `.planning/quick/260327-jt7-tag-dead-code/260327-jt7-SUMMARY.md`
</output>
