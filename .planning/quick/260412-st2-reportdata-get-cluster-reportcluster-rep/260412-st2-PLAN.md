---
phase: quick-260412-st2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/report/models.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ReportData.get_cluster(name) returns the first matching ReportCluster"
    - "Search is recursive through nested ReportCluster.children"
    - "Returns None when no cluster with given name is found"
  artifacts:
    - path: src/application/report/models.py
      provides: ReportData.get_cluster method implementation
      contains: "def get_cluster"
  key_links:
    - from: ReportData
      to: ReportCluster
      via: recursive search through clusters dict and children
      pattern: "get_cluster.*children"
---

<objective>
Add a recursive `get_cluster` method to ReportData that searches through nested ReportCluster structures.
</objective>

<context>
@src/application/report/models.py

## Data Model Structure

ReportData.clusters: dict[str, list[ReportCluster]]
ReportCluster.children: list[ReportCluster]  # Can be nested
ReportCluster.articles: list[ReportArticle]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_cluster method to ReportData</name>
  <files>src/application/report/models.py</files>
  <action>
Add a `get_cluster` method to the `ReportData` class that:
1. Searches through `self.clusters.values()` for a `ReportCluster` with matching `name`
2. Recursively searches through `ReportCluster.children` for nested clusters
3. Returns the first matching `ReportCluster` or `None` if not found

Implementation approach:
- Create a helper method `_find_cluster_in_list` that searches a list of ReportCluster recursively
- The public method iterates over `self.clusters.values()` and calls the helper
- Use `cluster.name == cluster_name` for exact matching

```python
def get_cluster(self, cluster_name: str) -> ReportCluster | None:
    """Get the first cluster with the given name, searching recursively.

    Args:
        cluster_name: Name of the cluster to find

    Returns:
        The first matching ReportCluster, or None if not found
    """
    for cluster_list in self.clusters.values():
        result = self._find_cluster_in_list(cluster_list, cluster_name)
        if result is not None:
            return result
    return None

def _find_cluster_in_list(
    self, clusters: list[ReportCluster], name: str
) -> ReportCluster | None:
    """Recursively find a cluster by name in a list of clusters."""
    for cluster in clusters:
        if cluster.name == name:
            return cluster
        # Search in children
        if cluster.children:
            result = self._find_cluster_in_list(cluster.children, name)
            if result is not None:
                return result
    return None
```
</action>
  <verify>
    <automated>grep -n "def get_cluster" src/application/report/models.py</automated>
  </verify>
  <done>ReportData.get_cluster(name) method exists and performs recursive search</done>
</task>

</tasks>

<verification>
grep -n "def get_cluster\|def _find_cluster_in_list" src/application/report/models.py
</verification>

<success_criteria>
- get_cluster method exists on ReportData class
- Method searches recursively through ReportCluster.children
- Returns ReportCluster | None
</success_criteria>

<output>
After completion, create `.planning/quick/260412-st2-reportdata-get-cluster-reportcluster-rep/260412-st2-SUMMARY.md`
</output>
