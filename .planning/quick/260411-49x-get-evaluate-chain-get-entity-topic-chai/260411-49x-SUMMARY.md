# Quick Task 260411-49x: JSON Mode for evaluate/entity_topic/tldr Chains

**Completed:** 2026-04-10
**Commit:** b3ff523
**Status:** Verified ✓

## Changes

### `src/llm/chains.py`

| Chain | 改动 |
|-------|------|
| `get_evaluate_chain()` | + `response_format={"type": "json_object"}`，简化 system message 移除 "Return ONLY valid JSON" |
| `get_entity_topic_chain()` | + `response_format={"type": "json_object"}`，简化 system message 移除 "Return ONLY valid JSON" |
| `get_tldr_chain()` | + `response_format={"type": "json_object"}`，prompt 无需改动 |

## JSON Mode 覆盖

现在所有使用 `JsonOutputParser` 的 chain 均已启用 litellm JSON mode：

| Chain | JSON mode | Pipeline |
|-------|-----------|----------|
| `get_evaluate_chain()` | ✅ | `EVALUATE_PROMPT \| _get_llm_wrapper(..., {"type": "json_object"}) \| JsonOutputParser()` |
| `get_entity_topic_chain()` | ✅ | `ENTITY_TOPIC_PROMPT \| _get_llm_wrapper(150, {"type": "json_object"}) \| JsonOutputParser()` |
| `get_tldr_chain()` | ✅ | `TLDR_PROMPT \| _get_llm_wrapper(300, {"type": "json_object"}) \| JsonOutputParser()` |
| `get_ner_chain()` | ✅ (260411-42p) | `NER_PROMPT \| _get_llm_wrapper(200, {"type": "json_object"}) \| JsonOutputParser()` |
| `get_translate_chain()` | ❌ (StrOutputParser) | `TRANSLATE_PROMPT \| _get_llm_wrapper(...) \| StrOutputParser()` |

## Verification

```bash
uv run python -c "
from src.llm.chains import get_evaluate_chain, get_entity_topic_chain, get_tldr_chain
get_evaluate_chain()
get_entity_topic_chain()
get_tldr_chain()
with open('src/llm/chains.py') as f:
    count = f.read().count('\"type\": \"json_object\"')
print(f'json_object occurrences: {count} (expected 4)')
"
# Output: json_object occurrences: 4 (expected 4)
```
