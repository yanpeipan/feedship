---
phase: quick-260411-49x
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/chains.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "get_evaluate_chain() uses JSON mode via response_format"
    - "get_entity_topic_chain() uses JSON mode via response_format"
    - "get_tldr_chain() uses JSON mode via response_format"
  artifacts:
    - path: src/llm/chains.py
      provides: "3 chains with JSON mode enabled"
  key_links: []
---

<objective>
Enable JSON mode on 3 chains (get_evaluate_chain, get_entity_topic_chain, get_tldr_chain) by passing `response_format={"type": "json_object"}` to `_get_llm_wrapper()`. Simplify system prompts by removing manual JSON constraints since litellm enforces it.
</objective>

<context>
@src/llm/chains.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable JSON mode on get_evaluate_chain()</name>
  <files>src/llm/chains.py</files>
  <action>
In `get_evaluate_chain()` (line 163-172):
1. Change `_get_llm_wrapper(MAX_TOKENS_PER_CHAIN["evaluate"])` to `_get_llm_wrapper(MAX_TOKENS_PER_CHAIN["evaluate"], {"type": "json_object"})`
2. Simplify EVALUATE_PROMPT system message: Change "You are a professional news report editor. Evaluate report quality objectively. Return ONLY valid JSON." to "You are a professional news report editor. Evaluate report quality objectively."
</action>
  <verify>
    <automated>grep -n 'response_format.*json_object.*get_evaluate_chain\|get_evaluate_chain.*json_object' src/llm/chains.py</automated>
  </verify>
  <done>get_evaluate_chain() passes response_format={"type": "json_object"} to _get_llm_wrapper, and EVALUATE_PROMPT system message no longer mentions JSON</done>
</task>

<task type="auto">
  <name>Task 2: Enable JSON mode on get_entity_topic_chain()</name>
  <files>src/llm/chains.py</files>
  <action>
In `get_entity_topic_chain()` (line 242-244):
1. Change `_get_llm_wrapper(150)` to `_get_llm_wrapper(150, {"type": "json_object"})`
2. Simplify ENTITY_TOPIC_PROMPT system message: Remove the sentence "Return ONLY valid JSON. " from the system message (keep everything else about layers and signals)
</action>
  <verify>
    <automated>grep -n 'response_format.*json_object.*get_entity_topic_chain\|get_entity_topic_chain.*json_object' src/llm/chains.py</automated>
  </verify>
  <done>get_entity_topic_chain() passes response_format={"type": "json_object"} to _get_llm_wrapper, and ENTITY_TOPIC_PROMPT system message no longer mentions JSON</done>
</task>

<task type="auto">
  <name>Task 3: Enable JSON mode on get_tldr_chain()</name>
  <files>src/llm/chains.py</files>
  <action>
In `get_tldr_chain()` (line 264-266):
1. Change `_get_llm_wrapper(300)` to `_get_llm_wrapper(300, {"type": "json_object"})`
2. TLDR_PROMPT system message does not mention JSON, so no prompt change needed
</action>
  <verify>
    <automated>grep -n 'response_format.*json_object.*get_tldr_chain\|get_tldr_chain.*json_object' src/llm/chains.py</automated>
  </verify>
  <done>get_tldr_chain() passes response_format={"type": "json_object"} to _get_llm_wrapper</done>
</task>

</tasks>

<verification>
Verify all 3 chains have response_format={"type": "json_object"}:
- `grep -n 'response_format={"type": "json_object"}' src/llm/chains.py` should show 4 occurrences (1 for NER, 3 for the chains being modified)
</verification>

<success_criteria>
- get_evaluate_chain() uses _get_llm_wrapper(max_tokens, {"type": "json_object"})
- get_entity_topic_chain() uses _get_llm_wrapper(150, {"type": "json_object"})
- get_tldr_chain() uses _get_llm_wrapper(300, {"type": "json_object"})
- EVALUATE_PROMPT and ENTITY_TOPIC_PROMPT system messages no longer contain "Return ONLY valid JSON"
- No changes to translate_chain (uses StrOutputParser, not JsonOutputParser)
</success_criteria>

<output>
After completion, create `.planning/quick/260411-49x-get-evaluate-chain-get-entity-topic-chai/260411-49x-SUMMARY.md`
</output>
