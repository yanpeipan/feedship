---
phase: quick
plan: "260324-x3k"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/articles.py
  - src/config.py
  - src/crawl.py
  - src/application/articles.py
  - src/application/config.py
  - src/application/crawl.py
  - src/application/feed.py
  - src/ai_tagging.py
  - src/providers/rss_provider.py
  - src/providers/github_release_provider.py
  - src/db.py
  - src/cli/article.py
  - src/cli/crawl.py
  - tests/test_config.py
autonomous: true
---

<objective>
Move src/articles.py, src/config.py, and src/crawl.py into the src/application/ module, following the existing application/feed.py pattern.
</objective>

<context>
@src/articles.py (382 lines - ArticleListItem, list_articles, get_article, get_article_detail, search_articles, list_articles_with_tags, get_articles_with_tags)
@src/config.py (25 lines - Dynaconf settings, get_timezone)
@src/crawl.py (321 lines - crawl_url, GitHub helpers, rate limiting, robots.txt)
@src/application/feed.py (303 lines - existing application module pattern)
@src/application/__init__.py (2 lines - "Application layer - use cases and orchestration")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy files to application module</name>
  <files>
    src/application/articles.py
    src/application/config.py
    src/application/crawl.py
  </files>
  <action>
    Copy the three files to src/application/ directory:
    - src/articles.py → src/application/articles.py
    - src/config.py → src/application/config.py
    - src/crawl.py → src/application/crawl.py

    No code changes - just file copying. The imports within these files already use relative imports like "from src.db import ..." which will work from the new location since the src/ root remains accessible.
  </action>
  <verify>
    <automated>python -c "from src.application.articles import list_articles; from src.application.config import get_timezone; from src.application.crawl import crawl_url; print('All imports OK')"</automated>
  </verify>
  <done>Three files exist in src/application/ with correct imports</done>
</task>

<task type="auto">
  <name>Task 2: Update all imports across the codebase</name>
  <files>
    src/application/feed.py
    src/ai_tagging.py
    src/providers/rss_provider.py
    src/providers/github_release_provider.py
    src/db.py
    src/cli/article.py
    src/cli/crawl.py
    tests/test_config.py
  </files>
  <action>
    Update imports in all files that reference the old module paths:

    For each file, replace:
    - "from src.articles import" → "from src.application.articles import"
    - "from src.config import" → "from src.application.config import"
    - "from src.crawl import" → "from src.application.crawl import"
    - "import src.articles" → "import src.application.articles"

    Files to update:
    - src/application/feed.py: from src.config import get_timezone
    - src/ai_tagging.py: from src.articles import get_article
    - src/providers/rss_provider.py: from src.config import get_timezone
    - src/providers/github_release_provider.py: from src.config import get_timezone
    - src/db.py: from src.config import get_timezone
    - src/cli/article.py: from src.articles import ...
    - src/cli/crawl.py: from src.crawl import crawl_url
    - tests/test_config.py: from src.config import get_timezone
  </action>
  <verify>
    <automated>python -c "
from src.application.articles import list_articles, get_article, get_article_detail, search_articles, list_articles_with_tags, get_articles_with_tags
from src.application.config import get_timezone
from src.application.crawl import crawl_url, ensure_crawled_feed, is_github_blob_url, is_github_commits_url
print('All application module imports OK')
"</automated>
  </verify>
  <done>All imports updated across codebase, no src.articles, src.config, src.crawl imports remain</done>
</task>

<task type="auto">
  <name>Task 3: Delete old files and verify no remaining references</name>
  <files>
    src/articles.py
    src/config.py
    src/crawl.py
  </files>
  <action>
    Delete the original files:
    - rm src/articles.py
    - rm src/config.py
    - rm src/crawl.py

    Then verify no remaining imports reference the old paths:
    grep -r "from src\.articles import" src/
    grep -r "from src\.config import" src/
    grep -r "from src\.crawl import" src/
  </action>
  <verify>
    <automated>grep -r "from src\.articles import\|from src\.config import\|from src\.crawl import" src/ --include="*.py" | grep -v "application/" || echo "No old imports found"</automated>
  </verify>
  <done>Old files deleted, no import references to old paths remain</done>
</task>

</tasks>

<verification>
All imports work correctly from new locations:
python -c "from src.application.articles import list_articles; from src.application.config import get_timezone; from src.application.crawl import crawl_url; print('Migration complete')"
</verification>

<success_criteria>
- src/application/articles.py, config.py, crawl.py exist with correct content
- All imports updated to use src.application.* paths
- Original files src/articles.py, src/config.py, src/crawl.py deleted
- No remaining references to old import paths in src/
- Python imports work correctly
</success_criteria>

<output>
After completion, create `.planning/quick/260324-x3k-articles-py-config-py-crawl-py-applicati/260324-x3k-SUMMARY.md`
</output>
