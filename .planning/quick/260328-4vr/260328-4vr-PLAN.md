---
phase: quick
plan: "260328-4vr"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/search.py
  - src/cli/article.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "print_articles has no mode parameter"
    - "search_articles_semantic return type remains list[dict] with distance/document"
  artifacts:
    - path: "src/application/search.py"
      provides: "print_articles without mode parameter, unified empty message and header"
  key_links:
    - from: "src/cli/article.py"
      to: "src/application/search.print_articles"
      via: "function call"
      pattern: "print_articles\\(formatted, verbose\\)"
---

<objective>
Simplify print_articles by removing the mode parameter and clarify search_articles_semantic return type.
</objective>

<context>
@src/application/search.py
@src/cli/article.py
@src/storage/vector.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update print_articles to remove mode parameter</name>
  <files>src/application/search.py</files>
  <action>
    In src/application/search.py, modify print_articles function (line 337):

    1. Remove `mode: str` parameter, keeping only `items` and `verbose`
    2. Replace the mode-branching empty message (lines 347-354) with unified message: "No articles found."
    3. Replace the headers dict (lines 357-362) with single unified header: "ID | Title | Source | Date | Score\n" + "-" * 80
    4. Update _print_article_verbose (line 371) to remove mode parameter - use unified verbose output showing: title, id, source, date, and if url/link present show those, if document_preview present show that
    5. Ensure the non-verbose print (line 368) continues to work - it already uses generic fields (id, title, source, date, score)

    Do NOT change format_articles, format_semantic_results, or any rank_* functions.
  </action>
  <verify>
    <automated>grep -n "def print_articles" src/application/search.py && grep -n "mode" src/application/search.py | head -20</automated>
  </verify>
  <done>print_articles(items, verbose) signature, unified empty message, unified header</done>
</task>

<task type="auto">
  <name>Task 2: Update article_search CLI calls to print_articles</name>
  <files>src/cli/article.py</files>
  <action>
    In src/cli/article.py, update all calls to print_articles:

    1. Line 55 (article_list): change `print_articles(formatted, 'list', verbose=verbose)` to `print_articles(formatted, verbose=verbose)`
    2. Line 125 (article_search semantic): change `print_articles(formatted, 'semantic', verbose=verbose)` to `print_articles(formatted, verbose=verbose)`
    3. Line 131 (article_search fts): change `print_articles(formatted, 'fts', verbose=verbose)` to `print_articles(formatted, verbose=verbose)`
  </action>
  <verify>
    <automated>grep -n "print_articles" src/cli/article.py</automated>
  </verify>
  <done>All print_articles calls in article.py use new signature without mode</done>
</task>

</tasks>

<verification>
- grep confirms no "mode" parameter in print_articles definition
- grep confirms all print_articles calls in article.py pass only (formatted, verbose=verbose)
- grep confirms search_articles_semantic still returns dicts with distance/document fields
</verification>

<success_criteria>
1. print_articles has no mode parameter (only items and verbose)
2. Empty output shows unified "No articles found." message
3. Header is unified: "ID | Title | Source | Date | Score"
4. article_search and article_list CLI commands work without mode parameter
5. search_articles_semantic return type remains list[dict] with distance/document fields (clarified: cannot be ArticleListItem due to rank_semantic_results requirements)
</success_criteria>

<output>
After completion, create `.planning/quick/260328-4vr/260328-4vr-SUMMARY.md`
</output>
