---
phase: quick-260412-3uh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/report/report_generation.py
  - src/application/report/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "NERExtractor is no longer imported or called in report_generation.py"
    - "src/application/report/__init__.py does not export NERExtractor"
    - "src/application/report/ner.py file is deleted"
    - "tests/application/report/test_ner.py is deleted"
    - "feedship report command runs without NER-related errors"
  artifacts:
    - path: "src/application/report/report_generation.py"
      provides: "Removed NERExtractor import and usage"
    - path: "src/application/report/__init__.py"
      provides: "Removed NERExtractor from exports"
    - path: "src/application/report/ner.py"
      provides: "DELETED"
    - path: "tests/application/report/test_ner.py"
      provides: "DELETED"
  key_links:
    - from: "src/application/report/report_generation.py"
      to: "src/application/report/ner.py"
      via: "import + ner.extract_batch() call"
      removed: true
---

<objective>
删除 NERExtractor 及其在 report pipeline 中的调用。NERExtractor 是 stub 实现（返回空 entities），EntityClusterer 已经能处理空 entities（按 feed_id 分组），因此删除是安全的。
</objective>

<context>
@src/application/report/report_generation.py (lines 10-19 import, 171 import inside function, 190-191 call)
@src/application/report/__init__.py (line 23 export, line 42 __all__)
@src/application/report/ner.py (要删除的文件)
@tests/application/report/test_ner.py (要删除的测试)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove NERExtractor from report_generation.py</name>
  <files>src/application/report/report_generation.py</files>
  <action>
    1. 从文件顶部的 import 块（lines 10-19）中删除 `NERExtractor` 的导入
    2. 从 `_entity_report_async` 函数内的 local import（line 171）中删除 `from src.application.report.ner import NERExtractor`
    3. 删除 lines 189-191 的 NER 调用块：
       ```python
       # Layer 1: NER + Enrich
       ner = NERExtractor(batch_size=5)
       enriched = await ner.extract_batch(filtered)
       ```
       改为直接将 filtered 赋值给 enriched：
       ```python
       # Layer 1: Enrich (pass-through until NER replacement)
       enriched = filtered
       ```
    注意：EntityClusterer.cluster() 在 entities 为空时会按 feed_id 分组，所以直接传 filtered 即可
  </action>
  <verify>
    <automated>grep -n "NERExtractor\|ner.extract_batch\|ner = NER" /Users/y3/feedship/src/application/report/report_generation.py; echo "Exit code: $?"</automated>
  </verify>
  <done>report_generation.py 中无 NERExtractor 引用，enriched = filtered 赋值存在</done>
</task>

<task type="auto">
  <name>Task 2: Remove NERExtractor from __init__.py exports</name>
  <files>src/application/report/__init__.py</files>
  <action>
    1. 删除 line 23: `from src.application.report.ner import NERExtractor`
    2. 从 `__all__` 列表（line 42）中删除 `"NERExtractor"`
  </action>
  <verify>
    <automated>grep -n "NERExtractor\|ner" /Users/y3/feedship/src/application/report/__init__.py; echo "Exit code: $?"</automated>
  </verify>
  <done>__init__.py 中无 NERExtractor 引用</done>
</task>

<task type="auto">
  <name>Task 3: Delete ner.py and test_ner.py files</name>
  <files>src/application/report/ner.py, tests/application/report/test_ner.py</files>
  <action>
    删除以下文件：
    - src/application/report/ner.py
    - tests/application/report/test_ner.py
  </action>
  <verify>
    <automated>ls /Users/y3/feedship/src/application/report/ner.py /Users/y3/feedship/tests/application/report/test_ner.py 2>&1 | grep -c "No such file"</automated>
  </verify>
  <done>ner.py 和 test_ner.py 已删除</done>
</task>

</tasks>

<verification>
运行 `grep -rn "NERExtractor" src/application/report/ tests/application/report/` 确认无残留引用。
</verification>

<success_criteria>
- grep 无 NERExtractor 残留
- ner.py 文件不存在
- test_ner.py 文件不存在
- feulship report 命令可正常启动（不需要实际运行完整 pipeline）
</success_criteria>

<output>
After completion, create `.planning/quick/260412-3uh-nerextractor-src-application-report-ner-/260412-3uh-SUMMARY.md`
</output>
