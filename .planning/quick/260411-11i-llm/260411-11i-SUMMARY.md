# Summary: 260411-11i-llm

## Task
Remove per-title translation from render_report. Translate entire final report in one LLM call instead.

## Changes

### src/application/report_generation.py
- Removed per-title translation block from `render_report` (lines 453-492)
- Removed collection of all_titles from layers, signals, and creation
- Removed `_translate_titles_batch_async` call and cache population
- `format_title` filter remains registered but titles now stay in original language

### src/cli/report.py
- Added `_render_and_translate_report()` async helper function
- Calls `render_report` with original titles (no per-title translation)
- Translates entire report in single `get_translate_chain().ainvoke()` call when language != "en"
- CLI command now calls this helper instead of `asyncio.run(render_report(...))` directly

## Verification
- `grep -n "all_titles\|_translate_titles_batch_async\|_title_translate_cache" src/application/report_generation.py | grep -v "^#\|cache_key\|import"` — no active per-title translation code
- `grep -n "translate_chain\|get_translate_chain" src/cli/report.py` — report-level translation present

## LLM Call Reduction
- Before: N titles = N LLM calls (e.g., ~500 titles = ~500 calls)
- After: 1 call for entire report

## Commit
`d510140` — refactor(report): remove per-title translation, translate entire report once
