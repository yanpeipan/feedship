---
phase: quick-260411-11i
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/y3/feedship/src/application/report_generation.py
  - /Users/y3/feedship/src/cli/report.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per-title translation removed from render_report"
    - "Final report translated in single LLM call after rendering"
    - "LLM title translation calls reduced from N to 1"
  artifacts:
    - path: "src/application/report_generation.py"
      provides: "render_report without per-title translation"
      lines: "453-492"
    - path: "src/cli/report.py"
      provides: "Single report-level translation call after render"
      lines: "111-113"
---

<objective>
Remove per-title translation from render_report. Translate entire final report in one LLM call instead.
</objective>

<context>
@src/application/report_generation.py
@src/cli/report.py

**Current flow (per-title translation):**
1. render_report() collects ALL titles needing translation
2. For each unique title: call LLM via _translate_titles_batch_async
3. Store in cache, use in Jinja2 filter
→ 假设有 500 个英文标题 = 500 次 LLM 调用

**Optimized flow (report-level translation):**
1. render_report() renders template with original titles (no translation)
2. CLI calls get_translate_chain() ONCE on full report text
3. TRANSLATE_PROMPT already preserves [Title](url) pattern
→ 500 次 → 1 次

**LLM Call Reduction:**
- Before: N titles = N LLM calls
- After: 1 call for entire report
- For 3333 articles with ~500 titles: ~499 calls saved
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove per-title translation from render_report</name>
  <files>/Users/y3/feedship/src/application/report_generation.py</files>
  <action>
    In `render_report()`, remove the entire per-title translation block (lines 453-492):
    - Remove `all_titles` collection loops
    - Remove `_translate_titles_batch_async` call
    - Remove cache population
    - Keep everything else (template rendering, format_title filter setup)

    The `format_title` filter is still registered but will be a no-op since titles stay in original language.
    Titles in markdown will be in their original language (e.g. English), then the full report gets translated once.
  </action>
  <verify>
    <automated>grep -n "all_titles\|_translate_titles_batch_async\|_title_translate_cache" /Users/y3/feedship/src/application/report_generation.py | grep -v "^#\|cache_key\|import"</automated>
  </verify>
  <done>Per-title translation removed from render_report</done>
</task>

<task type="auto">
  <name>Task 2: Add report-level translation in CLI</name>
  <files>/Users/y3/feedship/src/cli/report.py</files>
  <action>
    In `report.py` CLI command, after `render_report()` returns:

    ```python
    # Translate the entire report in one LLM call if target differs from content
    if language != "en":
        from src.llm.chains import get_translate_chain
        translate_chain = get_translate_chain()
        report_text = await translate_chain.ainvoke(
            {"text": report_text, "target_lang": language}
        )
    ```

    Note: Since render_report is sync (asyncio.run), we need to wrap in new async function.
    Add a helper async function in report.py to handle this, or restructure slightly.
  </action>
  <verify>
    <automated>grep -n "translate_chain\|get_translate_chain" /Users/y3/feedship/src/cli/report.py</automated>
  </verify>
  <done>Report-level translation added after render_report</done>
</task>

</tasks>

<success_criteria>
- Per-title translation removed from render_report (no _translate_titles_batch_async calls)
- Full report translated once after rendering
- For 3333 articles: title translation LLM calls reduced from ~500 to 1
</success_criteria>

<output>
After completion, create `.planning/quick/260411-11i-llm/260411-11i-SUMMARY.md`
</output>
