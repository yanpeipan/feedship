---
phase: quick
plan: "260412-7zy"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/report/report_generation.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "leverage_articles, business_articles, creation_articles code is removed"
  artifacts:
    - path: "src/application/report/report_generation.py"
      provides: "Removed keyword lists and classification functions"
---

<objective>
Remove unused leverage_articles, business_articles, creation_articles code from report_generation.py
</objective>

<execution_context>
@/Users/y3/feedship/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/application/report/report_generation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused keyword lists, classification functions, and usage</name>
  <files>src/application/report/report_generation.py</files>
  <action>
    Edit src/application/report/report_generation.py to remove the following:

    1. Delete the _LEVERAGE_KEYWORDS list (lines 39-62):
       - Remove lines 39-62: the entire `_LEVERAGE_KEYWORDS = [...]` list

    2. Delete the _BUSINESS_KEYWORDS list (lines 63-?):
       - Remove from line 63 through the end of the list (before _CREATION_KEYWORDS)

    3. Delete the _CREATION_KEYWORDS list (lines 84-?):
       - Remove from line 84 through the end of the list (before the functions)

    4. Delete the three classification functions:
       - Remove `_classify_signal_leverage` function (lines 107-116)
       - Remove `_classify_signal_business` function (lines 119-128)
       - Remove `_classify_creation` function (lines 131-140)

    5. Remove the usage code (lines 413-420):
       - Remove lines 413-420 which compute leverage_articles, business_articles, creation_articles
       - Remove signals_data and creation_data assignments

    6. Update return dict (lines 426-427):
       - Remove "signals": signals_data from return dict
       - Remove "creation": creation_data from return dict

    IMPORTANT: Do NOT change anything else. Only remove the specific lines mentioned above.
  </action>
  <verify>
    <automated>grep -E "_LEVERAGE_KEYWORDS|_BUSINESS_KEYWORDS|_CREATION_KEYWORDS|_classify_signal_leverage|_classify_signal_business|_classify_creation|leverage_articles|business_articles|creation_articles" src/application/report/report_generation.py</automated>
  </verify>
  <done>All three keyword lists, three classification functions, and their usages are removed from report_generation.py</done>
</task>

</tasks>

<verification>
grep should return no matches for any of the deleted patterns.
</verification>

<success_criteria>
- No references to _LEVERAGE_KEYWORDS, _BUSINESS_KEYWORDS, _CREATION_KEYWORDS remain
- No references to _classify_signal_leverage, _classify_signal_business, _classify_creation remain
- No references to leverage_articles, business_articles, creation_articles remain
- The file still parses correctly (no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/quick/260412-7zy-leverage-articles-business-articles-crea/260412-7zy-SUMMARY.md`
</output>
