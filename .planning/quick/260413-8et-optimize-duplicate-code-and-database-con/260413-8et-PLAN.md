---
name: 260413-8et-optimize-duplicate-code
description: Optimize duplicate code and database connection handling
status: pending
quick_id: 260413-8et
created: 2026-04-12
must_haves:
  - Connection caching to avoid repeated PRAGMA execution
  - Shared utility module for date functions
  - store_article refactored to single UPSERT
  - All imports moved to module top-level
---

## Task 1: Create shared utility module

**Files:**
- `src/storage/sqlite/utils.py` (NEW)

**Action:**
1. Create `src/storage/sqlite/utils.py`
2. Move these functions from `conn.py` and `articles.py`:
   - `_normalize_published_at()`
   - `_date_to_timestamp()`
   - `_date_to_timestamp_end()`
   - `_date_to_str()`
   - `_date_to_str_end()`
3. Import from `utils.py` in both `conn.py` and `articles.py`
4. Remove duplicate definitions

**Verify:** `python -c "from src.storage.sqlite.utils import _normalize_published_at; print('OK')"`

**Done:** Duplicate function definitions removed, imports consolidated

---

## Task 2: Add connection caching

**Files:**
- `src/storage/sqlite/conn.py`

**Action:**
1. Add module-level `_cached_connection = None`
2. Modify `_get_connection()` to cache and reuse connection
3. Add `_close_connection()` function for cleanup
4. Keep all PRAGMA settings (WAL, NORMAL, busy_timeout, cache_size)
5. Add thread-safety with `threading.Lock()`

**Verify:** `python -c "from src.storage.sqlite.conn import get_db; print('OK')"`

**Done:** Connection created once and reused

---

## Task 3: Refactor store_article to UPSERT

**Files:**
- `src/storage/sqlite/articles.py`

**Action:**
1. Replace SELECT + INSERT/UPDATE pattern with single UPSERT:
   ```sql
   INSERT INTO articles (id, feed_id, title, link, guid, ...)
   VALUES (?, ?, ?, ?, ?, ...)
   ON CONFLICT(guid) DO UPDATE SET
       title = excluded.title,
       content = excluded.content,
       ...
   ```
2. Remove separate UPDATE branch
3. Keep RETURNING clause if SQLite version supports

**Verify:** `python -m pytest tests/test_storage.py -k store_article -v`

**Done:** Single transaction instead of SELECT + write

---

## Task 4: Move imports to top-level

**Files:**
- `src/application/fetch.py`

**Action:**
1. Move `import json` to top of module (currently scattered)
2. Create `parse_feed_metadata()` helper function
3. Replace inline metadata parsing with helper call

**Verify:** `python -c "from src.application.fetch import fetch_all_async; print('OK')"`

**Done:** Clean imports, no inline imports in functions
