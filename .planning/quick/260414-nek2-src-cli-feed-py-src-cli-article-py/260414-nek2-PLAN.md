---
name: 260414-nek2
description: CLI不要包含业务逻辑：_build_feed_meta移至FeedMetaData，_format_date移至application层
type: quick
status: completed
date: 2026-04-14
quick_id: 260414-nek2
---

must_haves:
  - _build_feed_meta 从 feed.py 移至 FeedMetaData.from_discovered_feed() classmethod
  - _format_date 从 article.py 移至 application/config 时间格式化函数
  - feed.py 和 article.py 不再包含这些业务逻辑函数
---

## Task 1: Move _build_feed_meta to FeedMetaData class

**files:**
- src/cli/feed.py
- src/models/__init__.py (or wherever FeedMetaData is defined)

**action:**
1. 在 FeedMetaData 类中添加 `@classmethod from_discovered_feed(cls, feed)` 方法
2. 将 `_build_feed_meta` 逻辑移入该 classmethod
3. 在 feed.py 中: 移除 `_build_feed_meta` 函数定义，改为从 FeedMetaData 导入 `from_discovered_feed`
4. 更新 feed.py 中所有 `_build_feed_meta(feed)` 调用为 `FeedMetaData.from_discovered_feed(feed)`

**verify:**
- `grep -n "_build_feed_meta" src/cli/` returns no results
- `grep -n "from_discovered_feed" src/cli/feed.py` shows 3 usages

**done:** false

---

## Task 2: Move _format_date to application layer

**files:**
- src/cli/article.py
- src/application/config.py (or create formatting utility)

**action:**
1. 将 `_format_date` 移至 `src/application/config.py` 作为 `format_published_date(published_at)` 函数
2. 移除 `src.cli.article` 中的 `_format_date` 函数
3. 更新所有调用 `_format_date` 的地方改为使用 `from src.application.config import format_published_date`

**verify:**
- `grep -n "_format_date" src/cli/article.py` returns no results
- `grep -rn "_format_date" src/` shows only the new location

**done:** false
