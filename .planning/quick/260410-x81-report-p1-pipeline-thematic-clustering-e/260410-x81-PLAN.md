---
phase: quick
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - src/application/report_generation.py
autonomous: true
requirements: []
must_haves:
  truths:
    - Entity pipeline computes signals.leverage and signals.business via rule-based classification
    - Entity pipeline computes creation via rule-based classification
    - Thematic fallback removed from _entity_report_async
    - _cluster_articles_async no longer exists in the codebase
  artifacts:
    - path: src/application/report_generation.py
      provides: Unified entity-based pipeline with signal classification
      min_lines: 1
---

<objective>
Unify report pipeline by adding rule-based signal classification to entity pipeline and removing deprecated thematic pipeline. After this change, all reports use entity-based pipeline with signals (leverage/business) and creation classification computed via keyword matching on all entity topic sources.
</objective>

<execution_context>
@/Users/y3/feedship/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/application/report_generation.py (lines 59-125: keyword lists and classifiers, lines 742-919: thematic pipeline, lines 922-1046: entity pipeline)
</context>

<interfaces>
Key functions in src/application/report_generation.py:

- `_classify_signal_leverage(article: dict) -> bool` (line 419): Returns True if article contains leverage keywords in title/summary/description
- `_classify_signal_business(article: dict) -> bool` (line 431): Returns True if article contains business keywords
- `_classify_creation(article: dict) -> bool` (line 443): Returns True if article contains creation/educational keywords
- `_entity_report_async(...)` (line 922): The entity pipeline. Returns dict with keys: rendered, tldr_top10, layers, signals, creation, by_layer, by_dimension, entity_topics, date_range
- `LAYER_KEYS` (line 34): ["AI应用", "AI模型", "AI基础设施", "芯片", "能源"]

Current entity pipeline signals_data (line 1027):
    signals_data: dict[str, list] = {"leverage": [], "business": []}
    creation_data: list[dict] = []

After modification, entity pipeline should populate these from all_sources (flattened from entity_topic_dicts sources).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add signal classification to entity pipeline</name>
  <files>src/application/report_generation.py</files>
  <action>
In `_entity_report_async`, after line 1024 (after layers_data is built) and before line 1026 (the comment about Entity pipeline doesn't compute signals), replace the empty signals_data and creation_data with rule-based classification:

1. Collect all sources from entity_topic_dicts:
   ```python
   all_sources = []
   for topic_dict in entity_topic_dicts:
       all_sources.extend(topic_dict.get("sources", []))
   ```

2. Classify using existing functions (lines 419-452):
   - leverage: `leverage_articles = [a for a in all_sources if _classify_signal_leverage(a)]`
   - business: `business_articles = [a for a in all_sources if _classify_signal_business(a)]`
   - creation: `creation_articles = [a for a in all_sources if _classify_creation(a)]`

3. Build signals_data and creation_data:
   ```python
   signals_data = {"leverage": leverage_articles, "business": business_articles}
   creation_data = [{"name": "创作选题", "topics": []}] if creation_articles else []
   ```
   (Note: creation uses flat list for minimal change; re-clustering would add 2nd LLM call)
  </action>
  <verify>
    <automated>grep -n "_classify_signal_leverage\|_classify_signal_business\|_classify_creation" src/application/report_generation.py | head -20</automated>
  </verify>
  <done>signals.leverage and signals.business populated via keyword matching on entity sources; creation populated with flat list</done>
</task>

<task type="auto">
  <name>Task 2: Remove thematic fallback from entity pipeline</name>
  <files>src/application/report_generation.py</files>
  <action>
Remove the exception handler fallback block from `_entity_report_async` (lines 1041-1046):

```python
except Exception as e:
    logger.warning(f"Entity clustering failed: {e}")
    # Fall back to thematic clustering
    return await _cluster_articles_async(
        pre_fetched_articles, since, until, auto_summarize, target_lang
    )
```

Replace with a simple re-raise so errors propagate rather than silently falling back to deprecated thematic pipeline:

```python
except Exception as e:
    logger.error(f"Entity clustering failed: {e}")
    raise
```
  </action>
  <verify>
    <automated>grep -n "_cluster_articles_async" src/application/report_generation.py</automated>
  </verify>
  <done>No more fallback to thematic pipeline on entity pipeline errors</done>
</task>

<task type="auto">
  <name>Task 3: Remove deprecated thematic pipeline functions</name>
  <files>src/application/report_generation.py</files>
  <action>
Remove the following in order (careful with line ranges after each deletion):

1. Remove keyword lists and classifier functions (lines 59-125):
   - `_LEVERAGE_KEYWORDS`, `_BUSINESS_KEYWORDS`, `_CREATION_KEYWORDS`
   - `_keyword_overlap`
   - `_classify_signal_leverage`, `_classify_signal_business`, `_classify_creation`

2. Remove `_lang_name` and `_layer_names` helpers (lines 45-55) — these are only used by thematic pipeline

3. Remove `_cluster_articles_into_topics` (lines 139-719) — entire async function

4. Remove `_cluster_articles_async` (lines 742-919) — entire async function

After all deletions, ensure the file still has:
- `cluster_articles_for_report` function
- `_entity_report_async` function (modified in Tasks 1-2)
- `LAYER_KEYS`, `LAYER_NAMES`, `LANG_NAMES` constants
- All entity pipeline imports and helpers
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('src/application/report_generation.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>_cluster_articles_async and _cluster_articles_into_topics no longer exist in the file</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "_cluster_articles_async" src/application/report_generation.py` returns no matches
2. `grep "_cluster_articles_into_topics" src/application/report_generation.py` returns no matches
3. `grep "_classify_signal" src/application/report_generation.py` shows only the 3 function definitions (not removed, they're still used by entity pipeline)
4. `grep "signals_data = " src/application/report_generation.py` shows signals_data populated from all_sources
5. `python3 -c "import ast; ast.parse(open('src/application/report_generation.py').read()); print('Syntax OK')"` passes
</verification>

<success_criteria>
- Entity pipeline produces non-empty signals.leverage, signals.business, and creation when articles match keyword patterns
- Thematic pipeline code (_cluster_articles_async, _cluster_articles_into_topics) completely removed
- No fallback path to thematic pipeline in _entity_report_async
- File parses without syntax errors
- Unit tests pass (if any exist for these functions)
</success_criteria>

<output>
After completion, create `.planning/quick/260410-x81-report-p1-pipeline-thematic-clustering-e/260410-x81-SUMMARY.md`
</output>
