# Quick Task 260411-27t: SUMMARY

**Task:** 把 deduplicate_articles 接到 list_articles 和 SignalFilter 之间

**Date:** 2026-04-11
**Commit:** 4acb631
**Status:** ✅ Complete

## Changes

### 1. `src/storage/sqlite/articles.py`
- SELECT 增加 `a.content_hash, a.minhash_signature` 两列
- `_compute_article_item` 中设置 `content_hash` 和 `minhash_signature` 字段

### 2. `src/application/articles.py`
- `ArticleListItem` 增加 `content_hash: str | None` 和 `minhash_signature: bytes | None` 字段

### 3. `src/application/report/report_generation.py`
- 导入 `deduplicate_articles`
- `article_dicts` 构造时包含 `content_hash` 和 `minhash_signature`
- `cluster_articles_for_report()` 在调用 `_entity_report_async()` **之前**调用 `deduplicate_articles()`

## Effect

Report pipeline 现在在 SignalFilter 之前执行完整三重去重：

```
list_articles (从 SQLite 取，含 content_hash/minhash_signature)
    ↓
deduplicate_articles()  ← 新增
    L1: SHA256 exact dedup
    L2: MinHash LSH (Jaccard ≥ 0.85)
    L3: Embedding cosine (≥ 0.92)
    ↓
SignalFilter (规则过滤 + 内存 dedup)
    ↓
NERExtractor
    ↓
EntityClusterer
    ↓
TLDRGenerator
```

## Verification

```bash
feedship report --since 2026-04-01 --until 2026-04-07 --limit 3333
# 应在日志中看到: "Dedup removed X duplicate articles"
```
