---
phase: quick-260411-1rz
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/sqlite/llm.py
  - src/storage/sqlite/articles.py
  - src/storage/sqlite/__init__.py
  - src/storage/__init__.py
  - src/storage/sqlite/impl.py
  - src/cli/summarize.py
  - src/application/report/report_generation.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "list_articles_for_llm removed"
    - "list_articles extended with content, summary, feed_weight, feed_url fields"
    - "All callers updated to use list_articles"
---

<objective>
Merge list_articles_for_llm into list_articles. Remove unsummarized_only. Update all callers.
</objective>

<context>
**Key differences to resolve:**
- list_articles returns: id, feed_id, feed_name, title, link, guid, published_at, description, quality_score
- list_articles_for_llm returns: id, feed_id, feed_name, feed_weight, title, link, published_at, description, content, summary, quality_score, keywords, tags, summarized_at, feed_url
- list_articles has sort_by, on params; list_articles_for_llm does not
- list_articles_for_llm has unsummarized_only filter; list_articles does not

**Decision:** Extend list_articles SELECT to include: content, summary, feed_weight, feed_url. Remove unsummarized_only param from merged API. Callers that need filtering do it in application layer.

**Callers to update:**
1. src/cli/summarize.py — import list_articles_for_llm → list_articles (needs only id field)
2. src/application/report/report_generation.py — import list_articles_for_llm → list_articles
3. src/storage/__init__.py — remove list_articles_for_llm from re-exports
4. src/storage/sqlite/__init__.py — remove list_articles_for_llm from re-exports
5. src/storage/sqlite/impl.py — remove list_articles_for_llm from re-exports
6. src/storage/sqlite/llm.py — remove list_articles_for_llm function (delete it)
7. src/storage/sqlite/articles.py — extend list_articles SELECT to include content, summary, feed_weight, feed_url
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend list_articles SELECT</name>
  <files>src/storage/sqlite/articles.py</files>
  <action>
    In list_articles(), extend the SELECT to include: content, summary, feed_weight, feed_url

    Current SELECT (articles.py line ~509):
    SELECT a.id, a.feed_id, f.name as feed_name,
           a.title, a.link, a.guid, a.published_at, a.description,
           a.quality_score

    New SELECT:
    SELECT a.id, a.feed_id, f.name as feed_name, f.weight as feed_weight,
           a.title, a.link, a.guid, a.published_at, a.description, a.content,
           a.summary, f.url as feed_url
  </action>
  <verify>
    <automated>grep -n "content, summary, feed_weight" src/storage/sqlite/articles.py</automated>
  </verify>
  <done>list_articles SELECT extended</done>
</task>

<task type="name">Task 2: Remove list_articles_for_llm</name>
  <files>src/storage/sqlite/llm.py</files>
  <action>
    Delete the list_articles_for_llm function from llm.py (lines 111-188 approx)
  </action>
  <verify>
    <automated>grep -n "def list_articles_for_llm" src/storage/sqlite/llm.py</automated>
  </verify>
  <done>list_articles_for_llm function deleted</done>
</task>

<task type="auto">
  <name>Task 3: Update all imports and re-exports</name>
  <files>
    - src/storage/sqlite/__init__.py
    - src/storage/__init__.py
    - src/storage/sqlite/impl.py
    - src/cli/summarize.py
    - src/application/report/report_generation.py
  </files>
  <action>
    In each file:
    - Change import from "list_articles_for_llm" to "list_articles"
    - In __init__ files: remove "list_articles_for_llm" from imports and __all__
    - In summarize.py: also change the function call (no other changes needed since only id field is used)
    - In report_generation.py: change import and function call
  </action>
  <verify>
    <automated>grep -r "list_articles_for_llm" --include="*.py" src/ tests/ | wc -l</automated>
  </verify>
  <done>All imports updated</done>
</task>

</tasks>

<success_criteria>
- No references to list_articles_for_llm remain in codebase
- list_articles returns all needed fields (id, title, link, description, content, summary, quality_score, feed_weight, feed_url, published_at, feed_id)
- All callers work correctly
</success_criteria>

<output>
After completion, create SUMMARY.md at .planning/quick/260411-1rz-.../260411-1rz-SUMMARY.md
</output>
