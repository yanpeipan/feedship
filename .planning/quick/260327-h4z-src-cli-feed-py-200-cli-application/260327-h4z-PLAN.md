---
phase: quick-260327-h4z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/fetch.py
  - src/cli/feed.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLI fetch command displays progress bar during async fetch"
    - "CLI fetch --all uses fetch_all_async from application layer"
    - "CLI fetch <ids> uses fetch_ids_async from application layer"
    - "feed.py line count reduced from 337 to under 200"
  artifacts:
    - path: "src/application/fetch.py"
      provides: "fetch_ids_async async generator function"
      min_lines: 30
    - path: "src/cli/feed.py"
      provides: "Simplified fetch command with progress bar"
      max_lines: 200
  key_links:
    - from: "src/cli/feed.py"
      to: "src/application/fetch.py"
      via: "fetch_ids_async() import"
      pattern: "fetch_ids_async"
---

<objective>
Refactor src/cli/feed.py to stay under 200 lines by moving async fetch-by-ids business logic into src/application/fetch.py. CLI should only handle progress bar display and summary output.

Purpose: Follows the same pattern as 260327-ghv (article.py refactor). CLI is presentation layer, application layer handles business logic.
Output: feed.py ~200 lines, application/fetch.py gains fetch_ids_async()
</objective>

<context>
@/Users/y3/radar/src/cli/feed.py
@/Users/y3/radar/src/application/fetch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetch_ids_async to application/fetch.py</name>
  <files>src/application/fetch.py</files>
  <action>
    Add fetch_ids_async(ids: list[str], concurrency: int = 10) async generator function to src/application/fetch.py.

    This function should:
    - Take list of feed IDs and concurrency limit
    - Use asyncio.Semaphore for concurrency control
    - For each ID, call asyncio.to_thread(fetch_one, id) to run sync fetch_one in thread pool
    - Yield dicts with: feed_id, new_articles, error
    - Skip "Feed not found" errors gracefully (yield error but continue)

    Pattern: Similar to existing fetch_all_async() but operates on IDs instead of fetching all feeds.

    Do NOT include any Rich/progress bar imports - this is business logic only.
  </action>
  <verify>
    <automated>python -c "from src.application.fetch import fetch_ids_async; import inspect; print('fetch_ids_async exists:', callable(fetch_ids_async)); print('is async gen:', inspect.isasyncgenfunction(fetch_ids_async))"</automated>
  </verify>
  <done>fetch_ids_async() function exists in application/fetch.py as async generator</done>
</task>

<task type="auto">
  <name>Task 2: Refactor CLI fetch command to use application layer</name>
  <files>src/cli/feed.py</files>
  <action>
    Refactor the fetch command to use application layer functions:

    1. Remove inline async function run_fetch_ids_with_progress (lines 185-236)
    2. Remove inline async function run_fetch_with_progress (lines 269-310) - already mostly correct but can simplify
    3. Add import: from src.application.fetch import fetch_all_async, fetch_ids_async
    4. For IDs case: Call fetch_ids_async(ids, concurrency) and iterate with progress bar
    5. For --all case: Call fetch_all_async(concurrency=concurrency) and iterate with progress bar

    Keep the Rich Progress bar in CLI (it's presentation). The application layer yields results, CLI updates progress.

    Key: fetch_ids_async yields dicts with feed_id, new_articles, error. fetch_all_async yields dicts with feed_id, feed_name, new_articles, error.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/cli/feed.py').read()); print('Syntax OK')" && wc -l src/cli/feed.py | awk '{print "Lines:", $1}'</automated>
  </verify>
  <done>fetch command simplified, feed.py under 200 lines</done>
</task>

</tasks>

<verification>
- `python -c "from src.application.fetch import fetch_ids_async, fetch_all_async"` succeeds
- `wc -l src/cli/feed.py` shows under 200 lines
- `python -c "from src.cli.feed import fetch"` succeeds
- No inline async functions in CLI fetch command (run_fetch_ids_with_progress, run_fetch_with_progress removed)
</verification>

<success_criteria>
- src/application/fetch.py has new fetch_ids_async() async generator
- src/cli/feed.py has 2 tasks (import update + fetch command refactor)
- feed.py line count reduced from 337 to under 200
- No business logic (progress tracking, async iteration logic) remains in CLI layer
</success_criteria>

<output>
After completion, create `.planning/quick/260327-h4z-src-cli-feed-py-200-cli-application/260327-h4z-SUMMARY.md`
</output>
