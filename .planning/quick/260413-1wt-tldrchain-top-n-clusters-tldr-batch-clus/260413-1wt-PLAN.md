---
phase: quick
plan: 260413-1wt
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/report/tldr.py
  - src/llm/chains.py
autonomous: true
requirements:
  - quick-task
must_haves:
  truths:
    - "All clusters with articles generate TLDR summaries"
    - "Each cluster uses only top_n articles for TLDR generation"
    - "TLDR output maintains JSON format compatible with TldrJsonOutputParser"
  artifacts:
    - path: "src/application/report/tldr.py"
      provides: "TLDRChain with per-cluster top_n article filtering"
      contains: "top_n articles per cluster"
    - path: "src/llm/chains.py"
      provides: "Enhanced TLDR prompt with CEO + AI analyst perspective"
      contains: "CEO perspective"
  key_links:
    - from: "TLDRChain.ainvoke"
      to: "get_tldr_chain"
      via: "_build_topics_block with per-cluster top_n articles"
      pattern: "_build_topics_block.*top_n"
---

<objective>
Modify TLDRChain to: (1) generate TLDR for ALL clusters (remove cluster-level top_n filtering), (2) each cluster only passes top_n articles to LLM, (3) upgrade prompt to CEO + AI news analyst detailed summary perspective.
</objective>

<context>
@src/application/report/tldr.py
@src/llm/chains.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify TLDRChain to use per-cluster top_n articles</name>
  <files>src/application/report/tldr.py</files>
  <action>
    Modify TLDRChain in src/application/report/tldr.py:

1. **Remove cluster-level top_n filtering** (line 94-96): Delete `[: self.top_n]` slice after sorting clusters. ALL clusters with articles should proceed.

2. **Add per-cluster article filtering**: Before building topics_block, sort each cluster's articles by quality_weight (descending) and take top_n.

3. **Update `_build_topics_block` method**: It already accesses `cluster.articles[0]` for first article. Update to build topics_block using only top_n articles per cluster. The method receives cluster list, so for each cluster:
   - Sort cluster.articles by quality_weight descending
   - Take first top_n articles
   - Use those articles' translations/titles for the topics_block

4. **Update docstrings**: Change `top_n` parameter description from "limit number of clusters" to "limit articles per cluster for TLDR generation"

5. **Remove line 89-92 quality_weight sorting logic** for clusters (no longer needed since we process all clusters). Keep the per-cluster article sorting in _build_topics_block.

The key change in ainvoke:
- Before: sort clusters by quality_weight, take top_n clusters
- After: process ALL clusters, each cluster uses only top_n articles

</action>
  <verify>
    <automated>grep -n "top_n" src/application/report/tldr.py | head -20</automated>
  </verify>
  <done>TLDRChain processes all clusters, each cluster's topics_block built from only top_n articles</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade TLDR_PROMPT to CEO + AI analyst perspective</name>
  <files>src/llm/chains.py</files>
  <action>
    Modify TLDR_PROMPT and get_tldr_chain in src/llm/chains.py:

**TLDR_PROMPT change (lines 246-259):**

Replace the system prompt from:
"You are a news editor. Generate a 1-sentence TLDR for each entity topic. Focus on: what happened, why it matters."

To a multi-perspective prompt:
"You are a senior news analyst with dual perspectives:

1. **CEO Perspective**: Evaluate the business impact, strategic implications, market significance, and competitive landscape for each topic.
2. **AI/Technology Analyst Perspective**: Assess the technological innovations, AI/ML developments, industry trends, and technical breakthroughs.

For each entity topic:
- Provide a 2-3 sentence summary combining BOTH perspectives
- Highlight key insights that matter to business leaders AND tech professionals
- Focus on depth over breadth,挖掘 (deep dive) the most important aspects

Write in {target_lang}."

**Update the human message** to clarify the input format references top_n articles per cluster.

**Keep the JSON output format**: `[{"entity_id": "...", "tldr": "..."}]` - must remain compatible with TldrJsonOutputParser and TLDRItem model.

**get_tldr_chain max_tokens**: Increase from 300 to 800 (more detailed output needs more tokens).

</action>
  <verify>
    <automated>grep -n "CEO Perspective" src/llm/chains.py</automated>
  </verify>
  <done>TLDR_PROMPT generates detailed multi-perspective summaries, output compatible with TLDRItem</done>
</task>

</tasks>

<verification>
1. Read tldr.py and verify top_n filtering moved to per-cluster level
2. Read chains.py and verify new prompt contains CEO + AI analyst language
3. Verify get_tldr_chain max_tokens increased to 800
</verification>

<success_criteria>
- TLDRChain no longer slices clusters to top_n
- Each cluster passes only top_n (default 100) articles to topics_block
- TLDR_PROMPT system message mentions CEO perspective and AI analyst perspective
- get_tldr_chain uses 800 max_tokens
- JSON output format unchanged (compatible with TldrJsonOutputParser)
</success_criteria>

<output>
After completion, create `.planning/quick/260413-1wt-tldrchain-top-n-clusters-tldr-batch-clus/260413-1wt-SUMMARY.md`
</output>
