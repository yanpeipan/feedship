---
phase: quick
plan: 260325-0a9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db.py (moved to src/storage/sqlite.py)
  - src/storage/sqlite.py (new location)
  - src/application/crawl.py
  - src/application/articles.py
  - src/application/feed.py
  - src/tags/tag_rules.py
  - src/tags/ai_tagging.py
  - src/cli/__init__.py
  - src/cli/article.py
  - src/cli/tag.py
  - .planning/PROJECT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Database module is in src/storage/sqlite.py"
    - "_get_connection is truly private (no public alias)"
    - "All imports updated to use src.storage.sqlite"
    - "PROJECT.md documents the architectural decision"
  artifacts:
    - path: "src/storage/sqlite.py"
      provides: "SQLite database operations"
      min_lines: 350
    - path: "src/storage/__init__.py"
      provides: "Public API exports"
      min_lines: 10
  key_links:
    - from: "src/application/crawl.py"
      to: "src/storage/sqlite.py"
      via: "from src.storage.sqlite import get_db, store_article"
    - from: "src/cli/__init__.py"
      to: "src/storage/sqlite.py"
      via: "from src.storage.sqlite import init_db"
---

<objective>
Refactor db.py to src/storage/sqlite.py, make _get_connection truly private by removing the backwards compatibility alias, and update all imports. Document the architectural decision in PROJECT.md.
</objective>

<context>
@src/db.py (source file to move)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/storage/ directory and move db.py to sqlite.py</name>
  <files>src/storage/sqlite.py</files>
  <action>
    1. Create directory: src/storage/ (mkdir -p)
    2. Copy src/db.py to src/storage/sqlite.py
    3. In src/storage/sqlite.py:
       - Remove line 78: get_connection = _get_connection (the backwards compatibility alias)
       - Keep all other code intact
    4. Delete src/db.py
    5. Create src/storage/__init__.py with:
       from src.storage.sqlite import get_db, init_db, store_article, add_tag, list_tags, remove_tag, get_tag_article_counts, tag_article, untag_article, get_article_tags, get_db_path
  </action>
  <verify>ls src/storage/sqlite.py && python -c "from src.storage.sqlite import get_db; print('get_db OK')"</verify>
  <done>src/storage/sqlite.py exists without get_connection alias, src/storage/__init__.py exports main functions</done>
</task>

<task type="auto">
  <name>Task 2: Update imports in application modules</name>
  <files>src/application/crawl.py, src/application/articles.py, src/application/feed.py</files>
  <action>
    In src/application/crawl.py:
      Change: from src.db import get_db
      To: from src.storage.sqlite import get_db
      Change: from src.db import store_article
      To: from src.storage.sqlite import store_article

    In src/application/articles.py:
      Change: from src.db import get_db
      To: from src.storage.sqlite import get_db
      Change: from src.db import get_article_tags
      To: from src.storage.sqlite import get_article_tags
      Change: from src.db import store_article
      To: from src.storage.sqlite import store_article

    In src/application/feed.py:
      Change: from src.db import get_db
      To: from src.storage.sqlite import get_db
      Change: from src.db import store_article
      To: from src.storage.sqlite import store_article
  </action>
  <verify>python -c "from src.application.crawl import fetch_and_process_feed; from src.application.articles import get_articles_with_tags; from src.application.feed import refresh_feed; print('Application imports OK')"</verify>
  <done>All application module imports updated to src.storage.sqlite</done>
</task>

<task type="auto">
  <name>Task 3: Update imports in tags modules</name>
  <files>src/tags/tag_rules.py, src/tags/ai_tagging.py</files>
  <action>
    In src/tags/tag_rules.py:
      Change: from src.db import tag_article
      To: from src.storage.sqlite import tag_article

    In src/tags/ai_tagging.py:
      Change: from src.db import get_db, add_tag, tag_article
      To: from src.storage.sqlite import get_db, add_tag, tag_article
  </action>
  <verify>python -c "from src.tags.tag_rules import match_article_to_tags; from src.tags.ai_tagging import run_auto_tagging; print('Tags imports OK')"</verify>
  <done>Tags module imports updated to src.storage.sqlite</done>
</task>

<task type="auto">
  <name>Task 4: Update imports in CLI modules</name>
  <files>src/cli/__init__.py, src/cli/article.py, src/cli/tag.py</files>
  <action>
    In src/cli/__init__.py:
      Change: from src.db import init_db
      To: from src.storage.sqlite import init_db

    In src/cli/article.py:
      Change: from src.db import get_db
      To: from src.storage.sqlite import get_db
      Change: from src.db import tag_article
      To: from src.storage.sqlite import tag_article

    In src/cli/tag.py:
      Change: from src.db import add_tag, list_tags, remove_tag, get_tag_article_counts
      To: from src.storage.sqlite import add_tag, list_tags, remove_tag, get_tag_article_counts
  </action>
  <verify>python -c "from src.cli.article import cli; from src.cli.tag import tag_cli; print('CLI imports OK')"</verify>
  <done>CLI module imports updated to src.storage.sqlite</done>
</task>

<task type="auto">
  <name>Task 5: Document architectural decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
    In .planning/PROJECT.md Key Decisions table, add new row:
    | db.py to src/storage/sqlite | Database module moved to src/storage/ directory for better organization; _get_connection made truly private | | (v1.4) |
  </action>
  <verify>grep -A1 "db.py to src/storage" .planning/PROJECT.md</verify>
  <done>PROJECT.md Key Decisions table updated</done>
</task>

</tasks>

<verification>
python -c "
from src.storage.sqlite import get_db, init_db, store_article, add_tag, list_tags, remove_tag, get_tag_article_counts, tag_article, untag_article, get_article_tags, get_db_path
print('All sqlite storage imports OK')
"
python -c "from src.application.crawl import fetch_and_process_feed; print('crawl OK')"
python -c "from src.cli.article import cli; print('article CLI OK')"
python -c "from src.cli.tag import tag_cli; print('tag CLI OK')"
</verification>

<success_criteria>
- src/db.py deleted
- src/storage/sqlite.py exists with _get_connection (no public alias)
- src/storage/__init__.py exports main functions
- All imports updated from src.db to src.storage.sqlite
- PROJECT.md Key Decisions table updated
- No `from src.db import` remaining in src/ directory
</success_criteria>

<output>
After completion, create `.planning/quick/260325-0a9-db-py-src-storage-sqlite-get-connection/260325-0a9-SUMMARY.md`
</output>
