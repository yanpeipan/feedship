---
name: 260413-90j-langchain-optimization
description: LangChain LCEL optimization + report module refactor
status: pending
quick_id: 260413-90j
created: 2026-04-13
must_haves:
  - Phase 1: 4 critical bug fixes (write lock, event loop, freshness, docstring)
  - Phase 2: 3 LCEL migrations (BatchClassifyChain, TLDRChain, BuildReportDataChain)
  - Phase 3: Streaming dedup + generator pipeline refactor
  - Phase 4: Retry wrapper + final verification
---

## Phase 1: Critical Bug Fixes (No Behavioral Change)

### Task 1: Consolidate write lock to single source of truth

**Files:** `src/storage/sqlite/conn.py`, `src/storage/sqlite/articles.py`

Remove duplicate `_get_db_write_lock` from articles.py, import from conn.py instead.

**Verify:** `python -c "from src.storage.sqlite.articles import store_article_async; print('OK')"`

---

### Task 2: Fix event loop leaks in chain classes

**Files:** `src/application/report/classify.py`, `src/application/report/tldr.py`, `src/application/report/models.py`

Replace `asyncio.new_event_loop()` pattern with `asyncio.run()` in `invoke()`, `abatch()`, `batch()` methods.

**Verify:** `grep -n "new_event_loop" src/application/report/` returns no output

---

### Task 3: Fix list_articles freshness computation

**Files:** `src/storage/sqlite/articles.py`

Add `compute_freshness=False` parameter to `_compute_article_item()`. Only compute when `sort_by == "quality"`.

**Verify:** `uv run pytest tests/storage/sqlite/test_articles.py -v -x`

---

### Task 4: Fix utils.py misleading docstring

**Files:** `src/storage/sqlite/utils.py`

Fix `_date_to_str` docstring that incorrectly says "tz is ignored".

**Verify:** `python -c "from src.storage.sqlite.utils import _date_to_str; print(_date_to_str.__doc__)"`

---

## Phase 2: Migrate Custom Runnables to Standard LCEL

### Task 5: Migrate BatchClassifyChain to RunnableLambda

**Files:** `src/llm/chains.py`, `src/application/report/generator.py`, `src/application/report/classify.py`

Add `make_classify_runnable()` factory to chains.py. Update generator.py to use it.

**Verify:** `python -c "from src.application.report.generator import cluster_articles_for_report; print('Import OK')"`

---

### Task 6: Migrate TLDRChain to RunnableLambda

**Files:** `src/llm/chains.py`, `src/application/report/generator.py`, `src/application/report/tldr.py`

Add `make_tldr_runnable()` factory to chains.py.

**Verify:** `python -c "from src.llm.chains import make_tldr_runnable; print('OK')"`

---

### Task 7: Migrate BuildReportDataChain to RunnableLambda

**Files:** `src/llm/chains.py`, `src/application/report/generator.py`, `src/application/report/models.py`

Add `make_build_report_runnable()` factory to chains.py.

**Verify:** `python -c "from src.llm.chains import make_build_report_runnable; print('OK')"`

---

## Phase 3: Streaming Pipeline

### Task 8: Add streaming dedup to dedup.py

**Files:** `src/application/dedup.py`

Add `dedup_streaming()` and `minhash_dedup_streaming()` generator functions.

**Verify:** `uv run pytest tests/application/test_dedup.py -v -x`

---

### Task 9: Refactor generator.py to use streaming pipeline

**Files:** `src/application/report/generator.py`

Replace `_entity_report_async` with streaming pipeline using factory runnables.

**Verify:** `python -c "from src.application.report.generator import cluster_articles_for_report; print('OK')"`

---

### Task 10: Remove dedup logic from SignalFilter

**Files:** `src/application/report/filter.py`

Remove SHA256 dedup from `SignalFilter._passes_all_rules()` — now handled by dedup_streaming.

**Verify:** `uv run pytest tests/application/report/test_filter.py -v -x`

---

## Phase 4: Polish

### Task 11: Add LCEL retry wrapper for LLM calls

**Files:** `src/llm/chains.py`

Add `.with_retry(stop_after_attempt=2, retry_on=["RateLimitError", "APITimeoutError"])` to `get_translate_chain`, `get_tldr_chain`, `get_classify_translate_chain`.

**Verify:** `python -c "from src.llm.chains import get_translate_chain, get_tldr_chain; print('OK')"`

---

### Task 12: Final verification and re-index

**Files:** None

Run full test suite, smoke test report command, re-index GitNexus.

**Verify:** All tests pass, report command works, GitNexus re-indexed
