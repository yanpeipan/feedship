# Quick Task 260411-42p: 参考litellm最佳实践，使用JSON格式 - Context

**Gathered:** 2026-04-10
**Status:** Ready for planning

<domain>
## Task Boundary

参考 litellm JSON mode 最佳实践，将 LLM chain 切换为 JSON mode 输出格式，减少对正则提取 JSON 的依赖。

</domain>

<decisions>
## Implementation Decisions

### 应用范围
- NER chain 先行，仅在 NER chain 启用 JSON mode，验证有效后再推广到其他 chain

### 降级策略
- 当 model 不支持 JSON mode 时，抛出异常，不静默降级

### Claude's Discretion
- 具体实现细节（API 参数名、代码位置）由 planner 决定

</decisions>

<specifics>
## Specific Ideas

- 参考 https://docs.litellm.ai/docs/completion/json_mode
- litellm 支持 `response_format` 参数指定 `{"type": "json_object"}` 或 `{"type": "json_schema", "json_schema: {...}"}`
- NER chain 当前使用 `_extract_json` 正则提取，切换后可直接依赖 LLM 输出结构化 JSON

[If none: "No specific requirements — open to standard approaches"]

</specifics>

<canonical_refs>
## Canonical References

- https://docs.litellm.ai/docs/completion/json_mode (litellm 官方 JSON mode 文档)

[If none: "No external specs — requirements fully captured in decisions above"]

</canonical_refs>
