---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/sqlite.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "feeds table is created with weight column in single CREATE TABLE statement"
    - "No ALTER TABLE migration pattern exists in init_db"
  artifacts:
    - path: "src/storage/sqlite.py"
      provides: "init_db() function with direct schema definition"
      min_lines: 50
  key_links:
    - from: "src/storage/sqlite.py"
      to: "feeds table"
      via: "CREATE TABLE statement includes weight column"
      pattern: "CREATE TABLE.*feeds.*weight"
---

<objective>
Remove migration pattern from init_db() and sync the feeds table schema directly in CREATE TABLE.

Purpose: Clean schema definition without runtime ALTER TABLE migrations.
Output: Modified init_db() with weight column directly in feeds CREATE TABLE.
</objective>

<context>
@src/storage/sqlite.py (lines 90-156: init_db function)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ALTER TABLE migration, add weight to CREATE TABLE</name>
  <files>src/storage/sqlite.py</files>
  <action>
    In init_db() function, modify the feeds CREATE TABLE statement to include weight column directly:
    - Add `weight REAL DEFAULT 0.3` to the feeds CREATE TABLE columns
    - Remove the separate ALTER TABLE block (lines 149-154) that adds weight via migration

    The feeds CREATE TABLE should end up with these columns:
    - id TEXT PRIMARY KEY
    - name TEXT NOT NULL
    - url TEXT NOT NULL UNIQUE
    - etag TEXT
    - last_modified TEXT
    - last_fetched TEXT
    - created_at TEXT DEFAULT CURRENT_TIMESTAMP
    - weight REAL DEFAULT 0.3

    Keep the try/except pattern ONLY if there are other migrations; otherwise remove the entire migration block.
  </action>
  <verify>
    <automated>python -c "from src.storage.sqlite import init_db; print('init_db imports successfully')"</automated>
  </verify>
  <done>feeds table schema includes weight column directly, no ALTER TABLE migration for weight exists</done>
</task>

</tasks>

<verification>
- grep finds no "ALTER TABLE" in init_db for weight column
- CREATE TABLE for feeds includes weight column definition
</verification>

<success_criteria>
- init_db() creates feeds table with weight column in single CREATE TABLE statement
- No runtime migration (ALTER TABLE) for weight column
</success_criteria>

<output>
After completion, create `.planning/quick/260328-3og-remove-migration-init-db/260328-3og-SUMMARY.md`
</output>
