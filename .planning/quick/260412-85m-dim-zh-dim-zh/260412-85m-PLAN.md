---
phase: quick
plan: 260412-85m
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/report/render.py
  - src/application/report/__init__.py
  - src/application/report/report_generation.py
autonomous: true
requirements: []
must_haves:
  truths:
    - DIM_ZH constant no longer exists in render.py
    - dim_zh function no longer exists in render.py
    - dim_zh not imported or used in report_generation.py
    - dim_zh not exported from __init__.py
  artifacts:
    - path: src/application/report/render.py
      contains: "def dim_zh" — ABSENT
    - path: src/application/report/render.py
      contains: "DIM_ZH:" — ABSENT
  key_links: []
---

<objective>
删除 DIM_ZH 常量和 dim_zh 函数，清理所有相关引用。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 删除 DIM_ZH 和 dim_zh</name>
  <files>src/application/report/render.py</files>
  <action>
从 src/application/report/render.py 删除以下内容：
1. 第8-14行: DIM_ZH 字典常量
2. 第17-19行: dim_zh 函数定义

保留文件中其他所有内容不变。
  </action>
  <verify>
    <automated>grep -n "DIM_ZH\|def dim_zh" src/application/report/render.py || echo "DELETED"</automated>
  </verify>
  <done>DIM_ZH 和 dim_zh 已从 render.py 中删除</done>
</task>

<task type="auto">
  <name>Task 2: 移除 __init__.py 中的 dim_zh 导出</name>
  <files>src/application/report/__init__.py</files>
  <action>
从 src/application/report/__init__.py 删除：
1. 第22行: import 语句中的 dim_zh
2. 第44行: export 列表中的 "dim_zh"
  </action>
  <verify>
    <automated>grep -n "dim_zh" src/application/report/__init__.py || echo "DELETED"</automated>
  </verify>
  <done>dim_zh 已从 __init__.py 导出中移除</done>
</task>

<task type="auto">
  <name>Task 3: 移除 report_generation.py 中的 dim_zh 引用</name>
  <files>src/application/report/report_generation.py</files>
  <action>
从 src/application/report/report_generation.py 删除：
1. 第369行: from src.application.report.render import dim_zh
2. 第378行: env.filters["dim_zh"] = dim_zh
  </action>
  <verify>
    <automated>grep -n "dim_zh" src/application/report/report_generation.py || echo "DELETED"</automated>
  </verify>
  <done>dim_zh 引用已从 report_generation.py 中移除</done>
</task>

</tasks>

<verification>
python -c "from src.application.report import render; print('OK')"
</verification>

<success_criteria>
- DIM_ZH 和 dim_zh 不存在于 render.py
- dim_zh 不在 __init__.py 导出中
- report_generation.py 不引用 dim_zh
- 模块可正常导入
</success_criteria>

<output>
创建 .planning/quick/260412-85m-dim-zh-dim-zh/260412-85m-SUMMARY.md
</output>
