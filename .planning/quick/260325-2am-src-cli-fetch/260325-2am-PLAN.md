---
phase: quick-260325-2am-src-cli-fetch
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/feed.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can specify one or more URLs directly to fetch command"
    - "Each URL is crawled and stored via crawl_url()"
    - "Success/failure is reported for each URL"
  artifacts:
    - path: src/cli/feed.py
      provides: Modified fetch command with URL argument support
      contains: "@click.argument(\"urls\""
  key_links:
    - from: src/cli/feed.py
      to: src/application/crawl.py
      via: crawl_url function call
      pattern: "crawl_url"
---

<objective>
Add support for fetching one or more URLs directly via `fetch` command by accepting URL arguments.
</objective>

<context>
@src/cli/feed.py (current fetch implementation)
@src/application/crawl.py (crawl_url function for arbitrary URLs)
</context>

<tasks>

<task type="auto">
  <name>Task: Add URL argument support to fetch command</name>
  <files>src/cli/feed.py</files>
  <action>
    Modify the `fetch` command in src/cli/feed.py:

    1. Add `urls` as optional CLICK arguments after `--all` flag:
       ```python
       @cli.command("fetch")
       @click.option("--all", "do_fetch_all", is_flag=True, help="Fetch all feeds")
       @click.argument("urls", nargs=-1, required=False)
       @click.pass_context
       def fetch(ctx: click.Context, do_fetch_all: bool, urls: tuple) -> None:
       ```

    2. Import crawl_url: `from src.application.crawl import crawl_url`

    3. Handle three cases:
       - No args and not --all: Show help message (existing behavior)
       - --all flag: Use existing fetch_all() (existing behavior)
       - URL args provided: Loop through each URL and call crawl_url(url), print success/failure for each

    4. When crawling URLs:
       - Import ensure_crawled_feed at module level (lazy import to avoid circular deps)
       - Call crawl_url(url) for each URL
       - On success: print green "Fetched: {title} ({url})"
       - On failure: print red "Failed to fetch {url}: {reason}"
       - Return exit code 1 if any URL failed

    Do NOT modify the feed refresh logic - keep it separate.
  </action>
  <verify>
    <automated>python -c "from src.cli.feed import fetch; print('Import OK')"</automated>
  </verify>
  <done>fetch command accepts URL arguments and crawls each URL using crawl_url()</done>
</task>

</tasks>

<verification>
Manual test:
1. python -m src.cli fetch https://example.com (should crawl the URL)
2. python -m src.cli fetch https://example.com https://python.org (should crawl both)
3. python -m src.cli fetch --all (existing behavior unchanged)
4. python -m src.cli fetch (should show help message)
</verification>

<success_criteria>
- `src fetch <url>` fetches and stores the URL content
- `src fetch <url1> <url2>` fetches multiple URLs in sequence
- `src fetch --all` still works as before
- `src fetch` with no args shows existing help message
- Each fetched URL shows title and success message
</success_criteria>

<output>
After completion, create `.planning/quick/260325-2am-src-cli-fetch/260325-2am-SUMMARY.md`
</output>
