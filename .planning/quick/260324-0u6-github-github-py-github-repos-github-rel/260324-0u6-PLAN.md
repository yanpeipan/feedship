---
phase: quick
plan: "260324-0u6"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/github.py
  - src/db.py
  - src/articles.py
  - src/cli.py
  - src/db_migrations.py
  - src/tags.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/github.py file does not exist"
    - "db.py contains no github_repos, github_releases, or github_release_tags CREATE TABLE statements"
    - "articles.py has no JOIN or UNION ALL with github_releases or github_repos"
    - "cli.py has no import from src.github or repo command group"
    - "All modified files pass python -m py_compile"
  artifacts:
    - path: "src/github.py"
      deleted: true
    - path: "src/db.py"
      contains:
        - no "github_repos"
        - no "github_releases"
        - no "github_release_tags"
        - no "tag_github_release"
        - no "untag_github_release"
        - no "get_release_tags"
        - no "get_release_detail"
    - path: "src/articles.py"
      contains:
        - no "UNION ALL"
        - no "github_releases"
        - no "github_repos"
        - no "github_release_tags"
        - no "release_ids"
---

<objective>
Remove all GitHub-related dead code from the Python project. This includes deleting the entire github.py module, removing GitHub table definitions from db.py, cleaning GitHub JOINs from articles.py, removing GitHub commands from cli.py, and removing unused article_embeddings table creation from tags.py.
</objective>

<context>
@src/github.py (DELETE ENTIRE FILE)
@src/db.py (remove github_repos/github_releases/github_release_tags tables + functions)
@src/articles.py (remove UNION ALL queries with github tables)
@src/cli.py (remove repo command group and github imports)
@src/db_migrations.py (DB-02/DB-03 functions reference github_repos)
@src/tags.py (article_embeddings table - unused per task description)
</context>

<tasks>

<task type="auto">
  <name>Task 1: DELETE github.py and CLEAN db.py schema</name>
  <files>src/github.py, src/db.py, src/db_migrations.py</files>
  <action>
    **DELETE src/github.py** - Remove the entire file (808 lines)

    **EDIT src/db.py** to remove:
    - Lines 121-133: `CREATE TABLE github_repos` statement
    - Lines 135-148: `CREATE TABLE github_releases` statement
    - Lines 150-152: Indexes for github tables (`idx_github_releases_repo_id`, `idx_github_releases_published`)
    - Lines 178-189: `CREATE TABLE github_release_tags` + `idx_github_release_tags_tag_id` index
    - Lines 191-196: repo_id column migration (ALTER TABLE articles ADD COLUMN repo_id)
    - Lines 264-281: `get_tag_article_counts()` - replace body with simple article tag counting (remove UNION ALL with github_release_tags)
    - Lines 334-361: `tag_github_release()` function - DELETE
    - Lines 364-381: `untag_github_release()` function - DELETE
    - Lines 384-397: `get_release_tags()` function - DELETE
    - Lines 400-453: `get_release_detail()` function - DELETE
    - Lines 472-545: `store_article()` - remove `repo_id: Optional[str] = None` parameter and all repo_id handling

    **EDIT src/db_migrations.py**:
    - Remove or make no-op `migrate_github_repos_to_feeds()` function (keep function but add early return)
    - Remove or make no-op `migrate_drop_github_repos()` function (keep function but add early return)
  </action>
  <verify>
    python -m py_compile src/db.py src/db_migrations.py && echo "db.py and db_migrations.py compile OK"
    test ! -f src/github.py && echo "github.py deleted"
  </verify>
  <done>
    github.py file does not exist. db.py has no github_repos/github_releases/github_release_tags CREATE TABLE statements. db.py has no tag_github_release, untag_github_release, get_release_tags, get_release_detail functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLEAN articles.py GitHub JOINs</name>
  <files>src/articles.py</files>
  <action>
    **EDIT src/articles.py** to clean GitHub JOINs:

    **list_articles() function (lines 47-123):**
    - When feed_id is None (lines 79-100): Remove the UNION ALL with github_releases query
    - Keep only the feed articles query: `SELECT 'feed' as source_type... FROM articles a JOIN feeds f ON a.feed_id = f.id`
    - Update to not reference github_releases or github_repos

    **search_articles() function (lines 231-344):**
    - Remove lines 305-340: The LIKE search for github releases via `FROM github_releases r JOIN github_repos g`
    - Keep only FTS5 search for feed articles

    **list_articles_with_tags() function (lines 347-458):**
    - When no feed_id (lines 401-433): Remove UNION ALL with github query
    - Keep only feed articles query with tag filtering
    - When feed_id provided (lines 380-400): This part is already feed-only, keep as-is

    **get_articles_with_tags() function (lines 461-512):**
    - Remove `release_ids` parameter
    - Remove the `if release_ids:` block that handles github_release_tags
    - Keep only article tag fetching logic
    - Update ArticleListItem dataclass: remove repo_id, repo_name, release_tag fields (source_type always "feed")

    **ArticleListItem dataclass (lines 14-44):**
    - Remove fields: repo_id, repo_name, release_tag
    - Keep: id, feed_id, feed_name, title, link, guid, pub_date, description
    - source_type always "feed" now, can remove the field or keep for compatibility
  </action>
  <verify>
    python -m py_compile src/articles.py && echo "articles.py compiles OK"
  </verify>
  <done>
    articles.py has no UNION ALL queries with github_releases or github_repos. No LIKE searches against github tables. No release_ids handling.
  </done>
</task>

<task type="auto">
  <name>Task 3: CLEAN cli.py GitHub commands and tags.py</name>
  <files>src/cli.py, src/tags.py</files>
  <action>
    **EDIT src/cli.py** to remove GitHub code:

    **Remove import (lines 43-52):**
    - Remove entire `from src.github import (...)` block

    **Remove from db import (lines 22-32):**
    - Remove `get_release_tags` from imports

    **fetch command (lines 573-586):**
    - Remove lines 573-586: GitHub repo refresh block (`github_repos = list_github_repos()` etc.)

    **repo command group (lines 646-903):**
    - Delete entire `@cli.group() def repo(ctx)` and all subcommands: repo_add, repo_list, repo_remove, repo_refresh, repo_changelog
    - Delete helper functions: _show_repo_changelog, _display_changelog

    **article_tag command (lines 448-477):**
    - Remove the `cursor.execute("SELECT id FROM github_releases WHERE id = ?...")` check
    - Remove the `tag_github_release` call path
    - Keep only the `tag_article` path

    **article_view command (lines 260-279):**
    - Remove `from src.db import get_release_detail`
    - Remove the `if not article: release = get_release_detail(...)` block

    **article_open command (lines 349-379):**
    - Remove `from src.db import get_release_detail`
    - Remove the `if not article: release = get_release_detail(...)` block

    **EDIT src/tags.py** to remove article_embeddings:
    - Remove `_load_vec_extension()` function (lines 17-24)
    - Remove `_ensure_embeddings_table()` function (lines 27-41)
    - Remove `store_embedding()` function (lines 66-80)
    - Remove `get_article_embedding()` function (lines 83-95)
    - Remove `generate_embeddings_for_articles()` function (lines 104-123)
    - Remove `discover_clusters()` function (lines 126-168)
    - Update `run_auto_tagging()` to not use embeddings/clustering (or keep minimal stub)

    **Remove unused imports in tags.py:**
    - Remove `import sqlite3` if only used for extension loading
    - Remove `from sklearn.cluster import DBSCAN`
    - Remove `import numpy as np`
  </action>
  <verify>
    python -m py_compile src/cli.py src/tags.py && echo "cli.py and tags.py compile OK"
  </verify>
  <done>
    cli.py has no import from src.github, no repo command group, no list_github_repos calls. tags.py has no article_embeddings table creation.
  </done>
</task>

</tasks>

<verification>
python -m py_compile src/db.py src/articles.py src/cli.py src/db_migrations.py src/tags.py && test ! -f src/github.py && echo "All files compile and github.py deleted"
</verification>

<success_criteria>
- src/github.py file is deleted
- src/db.py contains no github_repos/github_releases/github_release_tags CREATE TABLE statements
- src/db.py contains no tag_github_release, untag_github_release, get_release_tags, get_release_detail functions
- src/articles.py contains no UNION ALL with github_releases or JOIN with github_repos
- src/articles.py contains no release_ids parameter handling
- src/cli.py contains no import from src.github
- src/cli.py contains no repo command group
- src/tags.py contains no article_embeddings table creation
- All modified files pass `python -m py_compile` without errors
</success_criteria>

<output>
After completion, no summary needed (quick task).
</output>
