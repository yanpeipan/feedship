---
phase: quick
plan: "260324-0u6"
type: summary
---

# Quick Task Summary: 260324-0u6

**Task:** Remove all GitHub-related dead code from the Python project.

**Completed:** 2026-03-24

## What Was Done

1. **Deleted `src/github.py`** (808 lines) - Entire GitHub API client module
2. **Cleaned `src/db.py`** - Removed `github_repos`, `github_releases`, `github_release_tags` CREATE TABLE statements; removed `tag_github_release`, `untag_github_release`, `get_release_tags`, `get_release_detail` functions; simplified `get_tag_article_counts`; removed `repo_id` column from `store_article`
3. **Cleaned `src/db_migrations.py`** - Made `migrate_github_repos_to_feeds` and `migrate_drop_github_repos` no-ops
4. **Cleaned `src/articles.py`** - Removed UNION ALL queries with github tables; simplified `ArticleListItem` dataclass (removed `source_type`, `repo_id`, `repo_name`, `release_tag` fields)
5. **Cleaned `src/cli.py`** - Removed entire `repo` command group (repo_add, repo_list, repo_remove, repo_refresh, repo_changelog); removed github imports; removed auto_tag block from article_tag command; removed `--auto`, `--eps`, `--min-samples` options from article_tag
6. **Cleaned `src/tags.py`** - Removed all embedding/clustering functions (`_load_vec_extension`, `_ensure_embeddings_table`, `store_embedding`, `get_article_embedding`, `generate_embeddings_for_articles`, `discover_clusters`, `suggest_tag_name_from_articles`); replaced `run_auto_tagging` with empty stub; removed unused imports (sqlite3, numpy, sklearn)
7. **Cleaned `src/models.py`** - Removed `GitHubRepo` and `GitHubRelease` dataclasses

## Verification

All modified files pass `python -m py_compile`:
- src/db.py ✓
- src/articles.py ✓
- src/cli.py ✓
- src/db_migrations.py ✓
- src/tags.py ✓
- src/models.py ✓

All success criteria met:
- `src/github.py` deleted
- No `github_repos/github_releases/github_release_tags` tables in db.py
- No `tag_github_release/untag_github_release/get_release_tags/get_release_detail` functions in db.py
- No UNION ALL with github tables in articles.py
- No `release_ids` parameter in articles.py
- No `from src.github` import in cli.py
- No `repo` command group in cli.py
- No `article_embeddings` table creation in tags.py

## Commit

```
4a2ecd0 clean: remove all GitHub-related dead code
14 files changed, 91 insertions(+), 1952 deletions(-)
delete mode 100644 src/github.py
```
