# 260329m10-SUMMARY: Dedupe Feed Types with trafilatura

## Objective
复用 trafilatura.feeds 已有的常量，消除硬编码重复

## Changes

### src/discovery/parser.py
- Replaced hardcoded FEED_TYPE_MAP with `from trafilatura.feeds import FEED_TYPES, BLACKLIST, LINK_VALIDATION_RE`
- Added `extract_feed_type()` helper that uses trafilatura's FEED_TYPES
- Uses LINK_VALIDATION_RE and BLACKLIST from trafilatura

### src/discovery/common_paths.py
- Replaced hardcoded FEED_CONTENT_TYPES tuple with `from trafilatura.feeds import FEED_TYPES`
- `FEED_CONTENT_TYPES` is now `tuple(FEED_TYPES)` for backward compatibility

### src/discovery/fetcher.py
- Removed local FEED_TYPE_MAP
- Added `from trafilatura.feeds import FEED_TYPES`
- Updated `validate_feed()` to use FEED_TYPES for MIME type checking
- Removed unused FEED_CONTENT_TYPES import

### src/discovery/deep_crawl.py
- Added `from trafilatura.feeds import FEED_TYPES`
- Removed local FEED_TYPE_MAP in `_validate_and_extract_title()`
- Updated to use FEED_TYPES for MIME type checking

## Benefits
- Single source of truth for feed MIME types (trafilatura)
- 19 MIME types supported by trafilatura vs 5 hardcoded
- Reduced code duplication
- Easier maintenance

## Verification
```
python3 -c "
from src.discovery.parser import parse_link_elements, extract_feed_type
from src.discovery.fetcher import validate_feed
from src.discovery.deep_crawl import deep_crawl
from src.discovery.common_paths import FEED_CONTENT_TYPES
print('All imports OK')
"
```
Result: All imports OK

Provider tests: 24 passed

## Commit
TODO: Commit after summary created
