---
phase: quick-260412-3e6
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/chains.py
  - src/llm/output_models.py
  - src/application/report/ner.py
  - src/application/report/entity_cluster.py
  - src/application/report/report_generation.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "get_classify_translate_chain uses ClassifyTranslateOutput (not ClassifyTranslateItem) as its pydantic_object"
    - "get_ner_chain and get_entity_topic_chain are removed from chains.py"
    - "ner.py no longer imports or uses get_ner_chain"
    - "entity_cluster.py no longer imports or uses get_entity_topic_chain"
    - "NERExtractor and EntityClusterer are removed from report_generation.py pipeline"
  artifacts:
    - path: "src/llm/chains.py"
      contains: "JsonOutputParser(pydantic_object=ClassifyTranslateOutput)"
    - path: "src/llm/chains.py"
      does_not_contain: "def get_ner_chain"
    - path: "src/llm/chains.py"
      does_not_contain: "def get_entity_topic_chain"
    - path: "src/application/report/ner.py"
      does_not_contain: "get_ner_chain"
    - path: "src/application/report/entity_cluster.py"
      does_not_contain: "get_entity_topic_chain"
  key_links:
    - from: "get_classify_translate_chain"
      to: "ClassifyTranslateOutput"
      via: "JsonOutputParser"
    - from: "report_generation.py"
      to: "NERExtractor, EntityClusterer"
      via: "removed from pipeline"
---

<objective>
Fix get_classify_translate_chain output model and remove deprecated chains that have no functional replacement. Since get_ner_chain and get_entity_topic_chain serve different purposes than classify_translate (NER extraction vs tag classification), removing them requires also removing their consumers from the report pipeline.

Purpose: Clean up deprecated chains that were never properly integrated. The NER + entity_topic pipeline was a separate approach from classify_translate and both exist independently. Remove the unused chains and their pipeline consumers.
</objective>

<execution_context>
@/Users/y3/feedship/.claude/get-shit-done/workflows/execute-plan.md
@/Users/y3/feedship/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/llm/chains.py (get_classify_translate_chain - uses wrong pydantic_object)
@src/llm/output_models.py (ClassifyTranslateItem, ClassifyTranslateOutput)
@src/application/report/ner.py (imports get_ner_chain - must be updated)
@src/application/report/entity_cluster.py (imports get_entity_topic_chain - must be updated)
@src/application/report/report_generation.py (_entity_report_async uses NERExtractor + EntityClusterer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix get_classify_translate_chain to use ClassifyTranslateOutput</name>
  <files>src/llm/chains.py, src/llm/output_models.py</files>
  <action>
In src/llm/chains.py, update get_classify_translate_chain():
- Change JsonOutputParser(pydantic_object=ClassifyTranslateItem) to JsonOutputParser(pydantic_object=ClassifyTranslateOutput)
- Change _make_json_schema_response_format argument from ClassifyTranslateItem.model_json_schema() to ClassifyTranslateOutput.model_json_schema()
- Change the schema name from "ClassifyTranslateItem" to "ClassifyTranslateOutput"

The prompt returns a JSON array of items. When using ClassifyTranslateItem directly, JsonOutputParser tries to parse the entire array as a single item. ClassifyTranslateOutput wraps the array as {items: [...]} which matches the actual LLM output structure.

In src/llm/output_models.py, ensure ClassifyTranslateOutput is exported in the imports at the top of chains.py.
</action>
  <verify>
<automated>cd /Users/y3/feedship && python -c "from src.llm.chains import get_classify_translate_chain; chain = get_classify_translate_chain('AI,模型,发布\n产品,融资\n', '1. Google发布Gemma 4\n2. OpenAI融资', 'zh'); print('Chain created successfully')"</automated>
  </verify>
  <done>get_classify_translate_chain uses ClassifyTranslateOutput as pydantic_object</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated chains and update their consumers</name>
  <files>src/llm/chains.py, src/application/report/ner.py, src/application/report/entity_cluster.py, src/application/report/report_generation.py</files>
  <action>
**Step A: Remove deprecated chains from chains.py**

Remove from src/llm/chains.py:
1. NER_PROMPT constant (~lines 237-251)
2. get_ner_chain() function (~lines 254-267)
3. ENTITY_TOPIC_PROMPT constant (~lines 271-283)
4. get_entity_topic_chain() function (~lines 286-298)

Remove NERArticle from the imports at the top of chains.py (line 18).

**Step B: Update ner.py to not use get_ner_chain**

In src/application/report/ner.py:
- Remove the import: `from src.llm.chains import get_ner_chain` (line 11)
- The NERExtractor class relies on get_ner_chain. Since NER extraction is not being replaced by classify_translate (they do different things), we need to decide what to do with this class.

Options for ner.py (choose based on whether the entity report pipeline is being deprecated):
- If keeping entity report: rewrite NERExtractor to use its own implementation or a new chain
- If removing entity report: simplify ner.py to a stub or remove it entirely

Given the task goal is dead code removal and the entity report pipeline uses NERExtractor + EntityClusterer together, the simplest approach is to:
- Make NERExtractor.extract_batch() return empty entities (stub) so the pipeline can still run without the LLM NER call
- The entity extraction will be skipped until a proper replacement is implemented

**Step C: Update entity_cluster.py to not use get_entity_topic_chain**

In src/application/report/entity_cluster.py:
- Remove the import: `from src.llm.chains import get_entity_topic_chain` (line 9)
- Make EntityClusterer.cluster() return entity topics with basic info (headline=entity_name, layer="AI应用") without calling the LLM chain

**Step D: Update report_generation.py**

No changes needed to report_generation.py - the NERExtractor and EntityClusterer classes still exist (with stubs) and the pipeline will work without the LLM calls.

This approach preserves the pipeline structure while removing the deprecated chain dependencies.
</action>
  <verify>
<automated>cd /Users/y3/feedship && python -c "from src.llm.chains import get_ner_chain" 2>&1 | grep -q "ImportError" && echo "get_ner_chain removed" || echo "get_ner_chain still exists"</automated>
</verify>
  <done>get_ner_chain and get_entity_topic_chain removed from chains.py; ner.py and entity_cluster.py no longer import them</done>
</task>

</tasks>

<verification>
- `python -c "from src.llm.chains import get_classify_translate_chain; print('OK')"` passes
- `python -c "from src.llm.chains import get_ner_chain"` raises ImportError
- `python -c "from src.llm.chains import get_entity_topic_chain"` raises ImportError
- `python -c "from src.application.report.ner import NERExtractor; print('NERExtractor OK')"` passes
- `python -c "from src.application.report.entity_cluster import EntityClusterer; print('EntityClusterer OK')"` passes
</verification>

<success_criteria>
- get_classify_translate_chain uses ClassifyTranslateOutput (not ClassifyTranslateItem)
- get_ner_chain is not importable from src.llm.chains
- get_entity_topic_chain is not importable from src.llm.chains
- ner.py does not import get_ner_chain
- entity_cluster.py does not import get_entity_topic_chain
- NERExtractor and EntityClusterer classes still exist and are importable (but may have stub implementations)
- chains.py imports only the models actually used
</success_criteria>

<output>
After completion, create `.planning/quick/260412-3e6-get-classify-translate-chain-get-ner-cha/260412-3e6-SUMMARY.md`
</output>
