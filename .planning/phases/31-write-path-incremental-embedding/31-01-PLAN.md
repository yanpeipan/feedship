---
phase: 31-write-path-incremental-embedding
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - src/storage/vector.py
  - src/storage/__init__.py
autonomous: true
requirements:
  - SEM-06

must_haves:
  truths:
    - "New articles stored via store_article_async() generate ChromaDB embedding"
    - "Embedding uses content field (fallback to description if short)"
    - "Embedding stored with article_id, title, and url metadata"
  artifacts:
    - path: src/storage/vector.py
      provides: add_article_embedding() function
      contains: def add_article_embedding
    - path: src/storage/__init__.py
      provides: export of add_article_embedding
      contains: add_article_embedding
  key_links:
    - from: src/application/fetch.py
      to: src/storage/vector.py
      via: import add_article_embedding
      pattern: "add_article_embedding"
---

<objective>
Add `add_article_embedding()` function to vector.py and export it from storage/__init__.py.

Purpose: Create the ChromaDB write function that will be called during article fetch.
Output: New function ready for integration in fetch.py.
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/y3/radar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/y3/radar/src/storage/vector.py
@/Users/y3/radar/src/storage/__init__.py
@/Users/y3/radar/src/models.py
@/Users/y3/radar/.planning/phases/31-write-path-incremental-embedding/31-CONTEXT.md

## Locked Decisions (D-08, D-10)

D-08: Create `add_article_embedding(article_id, title, content, url)` in src/storage/vector.py
- Takes article_id, title, content, url
- Embeds content (or title+description for short content)
- Adds to ChromaDB "articles" collection via `.add()`

D-10: Content for embedding
- Use article content field primarily; fall back to description if content is empty/short
- Title is stored as metadata, not as embedding text
- URL stored as metadata

## ChromaDB Collection Schema (from Phase 30 D-06)
- Collection "articles" with: id (article_id), content (embedding text), title, url

## ChromaDB add() API
- Collection `.add()` takes: ids (list), documents (list), metadatas (list)
- Each must be same length
- Documents are the text to embed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add add_article_embedding() function to vector.py</name>
  <files>src/storage/vector.py</files>
  <action>
    Add `add_article_embedding(article_id: str, title: str, content: str, url: str) -> None` function to src/storage/vector.py after `get_chroma_collection()`.

    Implementation:
    1. Get the "articles" collection via `get_chroma_collection()`
    2. Determine embedding text:
       - If content exists and len(content) >= 50 chars, use content
       - Otherwise, use title + " " + description (fallback)
       - If still no useful text, log warning and skip embedding
    3. Prepare metadata dict: {"title": title, "url": url}
    4. Call collection.add() with:
       - ids=[article_id]
       - documents=[embedding_text]
       - metadatas=[metadata]

    Per D-10: Embedding generation is CPU-bound, so wrap in asyncio.to_thread() at call site (not here). ChromaDB client is thread-safe for writes.
  </action>
  <verify>
    <automated>python -c "from src.storage.vector import add_article_embedding; print('import ok')"</automated>
  </verify>
  <done>add_article_embedding(article_id, title, content, url) exists in vector.py and can be imported</done>
</task>

<task type="auto">
  <name>Task 2: Export add_article_embedding from storage/__init__.py</name>
  <files>src/storage/__init__.py</files>
  <action>
    Add `add_article_embedding` to the imports from src.storage.vector in src/storage/__init__.py.

    Current imports from vector.py:
    ```
    from src.storage.vector import (
        get_chroma_collection,
        _get_embedding_function,
    )
    ```

    Add `add_article_embedding` to this list.
  </action>
  <verify>
    <automated>python -c "from src.storage import add_article_embedding; print('export ok')"</automated>
  </verify>
  <done>add_article_embedding is exported from src.storage package</done>
</task>

</tasks>

<verification>
After Plan 01 completes:
- `from src.storage import add_article_embedding` works
- `from src.storage.vector import add_article_embedding` works
- Function signature matches: (article_id: str, title: str, content: str, url: str) -> None
</verification>

<success_criteria>
Plan 01 complete when:
1. add_article_embedding() function exists in src/storage/vector.py
2. Function is exported from src/storage/__init__.py
3. Both import statements work without error
</success_criteria>

<output>
After completion, create `.planning/phases/31-write-path-incremental-embedding/31-01-SUMMARY.md`
</output>
