---
phase: 41-articlelistitem-semantic-search-core
plan: "41-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/articles.py
  - src/storage/vector.py
autonomous: true
requirements:
  - SEARCH-00
  - SEARCH-01
  - SEARCH-02
must_haves:
  truths:
    - ArticleListItem has vec_sim, bm25_score, freshness, source_weight, ce_score, final_score fields
    - search_articles_semantic returns ArticleListItem with vec_sim set to raw cosine similarity from ChromaDB
    - search_articles_semantic no longer crashes when pub_date is INTEGER unix timestamp
    - search_articles_semantic score is NOT a weighted combination (returns raw cos_sim directly)
  artifacts:
    - path: src/application/articles.py
      provides: ArticleListItem dataclass with 6 new fields
      contains: "vec_sim.*0.0"
    - path: src/storage/vector.py
      provides: search_articles_semantic with raw cos_sim return
      contains: "vec_sim=cos_sim"
  key_links:
    - from: src/storage/vector.py
      to: src/application/articles.py
      via: import ArticleListItem
      pattern: "from src.application.articles import ArticleListItem"
---

<objective>
Extend ArticleListItem with 6 scoring fields (vec_sim, bm25_score, freshness, source_weight, ce_score, final_score); fix P0 crash in search_articles_semantic when pub_date is INTEGER unix timestamp; remove hardcoded weighted scoring formula, returning raw cosine similarity instead.

Purpose: Foundation for v2.0 Route A scoring architecture. All subsequent phases (42-44) depend on these changes.
Output: Extended ArticleListItem dataclass; fixed search_articles_semantic returning raw cos_sim
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/y3/radar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/application/articles.py
@src/storage/vector.py

## Phase Context

**Phase Goal:** ArticleListItem extended with scoring fields; search_articles_semantic returns raw cos_sim without crashing

**Requirements:**
- SEARCH-00: Fix line 363 crash — datetime.fromisoformat() fails on INTEGER pub_date
- SEARCH-01: ArticleListItem extends 6 fields (vec_sim, bm25_score, freshness, source_weight, ce_score, final_score)
- SEARCH-02: search_articles_semantic removes hardcoded formula, returns raw cos_sim as vec_sim

**Locked Decisions (from 41-CONTEXT.md):**
- D-01: vec_sim=0.0, bm25_score=0.0, freshness=0.0, source_weight=0.3, ce_score=0.0, final_score=0.0
- D-02: Keep score field for backward compatibility; final_score is canonical sort field
- D-04: Return raw cos_sim as vec_sim (not combined score)
- D-06: cos_sim = 1 - distance / 2 (hnsw:space=cosine)
- D-07: Freshness/source_weight signals removed from storage layer
- D-09: Use _pub_date_to_timestamp() for ALL timestamp conversions
- D-10: days_ago = (now - pub_dt).days where pub_dt = datetime.fromtimestamp(pub_date_ts, tz=timezone.utc)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ArticleListItem with 6 scoring fields (SEARCH-01)</name>
  <files>src/application/articles.py</files>
  <read_first>
    - src/application/articles.py (current ArticleListItem at line 20)
  </read_first>
  <action>
    In src/application/articles.py, extend the ArticleListItem dataclass (starting at line 19) by adding 6 new fields BEFORE the existing score field (line 42).

    Add these fields with their default values:
    - vec_sim: float = 0.0
    - bm25_score: float = 0.0
    - freshness: float = 0.0
    - source_weight: float = 0.3
    - ce_score: float = 0.0
    - final_score: float = 0.0

    Keep the existing score: float = 1.0 field for backward compatibility.

    The updated dataclass fields in order should be:
    1. id: str
    2. feed_id: str
    3. feed_name: str
    4. title: Optional[str]
    5. link: Optional[str]
    6. guid: str
    7. pub_date: Optional[str]
    8. description: Optional[str]
    9. vec_sim: float = 0.0          # NEW
    10. bm25_score: float = 0.0     # NEW
    11. freshness: float = 0.0      # NEW
    12. source_weight: float = 0.3  # NEW
    13. ce_score: float = 0.0       # NEW
    14. final_score: float = 0.0    # NEW
    15. score: float = 1.0          # EXISTING, keep for backward compat
  </action>
  <acceptance_criteria>
    - ArticleListItem dataclass has 6 new fields: vec_sim, bm25_score, freshness, source_weight, ce_score, final_score
    - grep -n "vec_sim.*float" src/application/articles.py returns a match
    - grep -n "bm25_score.*float" src/application/articles.py returns a match
    - grep -n "freshness.*float" src/application/articles.py returns a match
    - grep -n "source_weight.*float = 0.3" src/application/articles.py returns a match
    - grep -n "ce_score.*float" src/application/articles.py returns a match
    - grep -n "final_score.*float" src/application/articles.py returns a match
    - grep -n "score.*float = 1.0" src/application/articles.py still exists (backward compat)
    - python3 -c "from src.application.articles import ArticleListItem; a = ArticleListItem(id='1', feed_id='2', feed_name='f', guid='g'); print(a.vec_sim, a.bm25_score, a.freshness, a.source_weight, a.ce_score, a.final_score)" prints "0.0 0.0 0.0 0.3 0.0 0.0"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/y3/radar && python3 -c "from src.application.articles import ArticleListItem; a = ArticleListItem(id='1', feed_id='2', feed_name='f', guid='g'); print('vec_sim:', a.vec_sim, 'bm25_score:', a.bm25_score, 'freshness:', a.freshness, 'source_weight:', a.source_weight, 'ce_score:', a.ce_score, 'final_score:', a.final_score)"</automated>
  </verify>
  <done>ArticleListItem has 6 new scoring fields with correct default values; score field retained for backward compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Fix pub_date crash and return raw cos_sim (SEARCH-00, SEARCH-02)</name>
  <files>src/storage/vector.py</files>
  <read_first>
    - src/storage/vector.py (search_articles_semantic at line 265, _pub_date_to_timestamp at line 30)
  </read_first>
  <action>
    In src/storage/vector.py, modify the search_articles_semantic function (starting at line 265):

    1. FIX P0 CRASH (lines 359-369): Replace the inline datetime.fromisoformat() call with _pub_date_to_timestamp()
       The problematic code is:
         pub_date = article_info.get("pub_date")
         freshness = 0.0
         if pub_date:
             try:
                 pub_dt = datetime.fromisoformat(pub_date.replace("Z", "+00:00"))  # CRASHES ON INTEGER
                 ...

       Replace with:
         pub_date = article_info.get("pub_date")
         pub_ts = _pub_date_to_timestamp(pub_date)  # Returns int or None
         freshness = 0.0
         if pub_ts:
             pub_dt = datetime.fromtimestamp(pub_ts, tz=timezone.utc)
             days_ago = (datetime.now(timezone.utc) - pub_dt).days
             freshness = max(0.0, 1.0 - days_ago / 30)

    2. REMOVE HARDCODE FORMULA (lines 376-377): Delete these lines:
         # Final score: 0.5 * similarity + 0.2 * freshness + 0.3 * source_weight
         score = 0.5 * cos_sim + 0.2 * freshness + 0.3 * source_weight

    3. UPDATE ArticleListItem CONSTRUCTION (lines 395-408): When creating ArticleListItem objects, pass raw cos_sim as vec_sim, NOT as score. The ArticleListItem construction should:
       - Set vec_sim=cos_sim (raw cosine similarity, 0-1 range)
       - Set score=r.get("score", 1.0) - but r["score"] no longer exists since we removed the formula, so use score=1.0 as default
       - Remove the "score" key from the ranked_results dict since we're not computing it anymore

       Updated ranked_results append:
         ranked_results.append({
             "sqlite_id": sqlite_id,
             "article_id": article_id,
             "feed_id": feed_id or "",
             "feed_name": article_info.get("feed_name") or "",
             "title": metadatas[i].get("title") if metadatas[i] else None,
             "url": metadatas[i].get("url") if metadatas[i] else None,
             "pub_date": pub_date,
             "distance": distance,
             # score removed - we no longer compute a combined score here
         })

       Updated ArticleListItem construction:
         result_items.append(ArticleListItem(
             id=r["sqlite_id"] or r["article_id"] or "",
             feed_id=r["feed_id"] or "",
             feed_name=r["feed_name"] or "",
             title=r.get("title"),
             link=r.get("url"),
             guid=r["sqlite_id"] or r["article_id"] or "",
             pub_date=r.get("pub_date"),
             description=None,
             vec_sim=cos_sim,  # Raw cosine similarity
         ))

    4. REMOVE SORT BY SCORE (line 392): Since we no longer have a combined score, change:
         ranked_results.sort(key=lambda x: x["score"], reverse=True)
       To simply:
         # Results are already in order from ChromaDB query (by similarity)
         # No additional sorting needed - ChromaDB returns ordered by distance
         pass

    NOTE: The freshness computation (lines 358-369 after fix) is for ChromaDB metadata filtering context only. ArticleListItem.freshness is NOT populated by this function - that happens in combine_scores() (Phase 43).
  </action>
  <acceptance_criteria>
    - grep -n "_pub_date_to_timestamp(pub_date)" src/storage/vector.py returns a match in search_articles_semantic
    - grep -n "datetime.fromisoformat" src/storage/vector.py does NOT appear in search_articles_semantic (lines 265-410)
    - grep -n "0.5 \* cos_sim" src/storage/vector.py does NOT exist (hardcoded formula removed)
    - grep -n "vec_sim=cos_sim" src/storage/vector.py returns a match
    - grep -n "0.5 \* similarity" src/storage/vector.py does NOT exist
    - The ArticleListItem construction passes only: id, feed_id, feed_name, title, link, guid, pub_date, description, vec_sim
    - python3 -c "from src.storage.vector import search_articles_semantic; print('Function loads without crash')" succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/y3/radar && python3 -c "from src.storage.vector import search_articles_semantic; print('search_articles_semantic loads OK')" && grep -n "_pub_date_to_timestamp(pub_date)" src/storage/vector.py | head -5</automated>
  </verify>
  <done>search_articles_semantic no longer crashes on INTEGER pub_date; returns raw cos_sim as vec_sim without weighted formula</done>
</task>

</tasks>

<verification>
1. ArticleListItem has 6 new fields with correct defaults (source_weight=0.3, others=0.0)
2. search_articles_semantic uses _pub_date_to_timestamp() instead of datetime.fromisoformat()
3. search_articles_semantic returns raw cos_sim as vec_sim (not combined score)
4. No hardcoded weighted formula (0.5*cos + 0.2*fresh + 0.3*weight) exists in vector.py
</verification>

<success_criteria>
Phase 41 is complete when:
1. ArticleListItem dataclass has vec_sim, bm25_score, freshness, source_weight, ce_score, final_score fields
2. search_articles_semantic returns ArticleListItem with vec_sim set to raw cosine similarity from ChromaDB
3. search_articles_semantic no longer crashes when pub_date is INTEGER unix timestamp (uses _pub_date_to_timestamp)
4. search_articles_semantic score is NOT a weighted combination (hardcoded formula at lines 376-377 removed)
</success_criteria>

<output>
After completion, create `.planning/phases/41-articlelistitem-semantic-search-core/41-01-SUMMARY.md`
</output>
