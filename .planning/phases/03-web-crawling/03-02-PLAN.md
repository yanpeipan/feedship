---
phase: 03-web-crawling
plan: "03-02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/cli.py
autonomous: true
requirements:
  - CLI-04
user_setup: []

must_haves:
  truths:
    - "User can run `crawl <url>` and see confirmation message"
    - "User can run `crawl <url> --ignore-robots` to bypass robots.txt"
    - "Crawled content appears in `article list` output"
    - "Crawled content is searchable via `article search`"
  artifacts:
    - path: src/cli.py
      provides: crawl command with --ignore-robots option
  key_links:
    - from: src/cli.py
      to: src/crawl.py
      via: import and call crawl_url()
    - from: crawl command output
      to: user
      via: click.echo with success/error messages
---

<objective>
Add the `crawl` CLI command to src/cli.py that wraps crawl_url() from src/crawl.py. Command accepts a URL argument and optional --ignore-robots flag. Displays success message with article title or error message.
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/cli.py
@src/crawl.py
@src/articles.py
@Users/y3/radar/.planning/phases/03-web-crawling/03-CONTEXT.md

## Locked Decisions (must honor)

- D-06: `crawl <url>` with optional `--ignore-robots` flag
- D-05: Log and skip error handling (CLI should echo errors in red)

## Existing CLI Patterns (from cli.py)

Commands use click framework with this structure:
- @cli.command("name") decorator
- @click.argument for positional args
- @click.option for flags
- @click.pass_context for context
- click.echo(f"message", fg="green") for success
- click.echo(f"Error: {e}", err=True, fg="red") for errors
- sys.exit(1) on failure

## Expected Output Format

Success: "Crawled: {title} ({url})" in green
Error: "Error: Failed to crawl {url}: {reason}" in red
No articles found: "No content extracted from {url}" in yellow
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add crawl command to cli.py</name>
  <files>src/cli.py</files>
  <read_first>
    - src/cli.py (to see existing command patterns)
    - src/crawl.py (to understand crawl_url() return type and behavior)
  </read_first>
  <action>
    Add to src/cli.py:

    ## New imports (add to existing imports around line 14)
    from src.crawl import crawl_url

    ## New command (add after the fetch command, around line 285)
    @cli.command("crawl")
    @click.argument("url")
    @click.option("--ignore-robots", is_flag=True, help="Ignore robots.txt and crawl anyway (lazy mode disabled)")
    @click.pass_context
    def crawl(ctx: click.Context, url: str, ignore_robots: bool) -> None:
        """Fetch and store content from a URL as an article.

        Uses Readability algorithm to extract article content from webpages.
        Respects robots.txt by default (use --ignore-robots to override).

        Examples:

            rss-reader crawl https://example.com/article

            rss-reader crawl https://example.com --ignore-robots
        """
        verbose = ctx.parent and ctx.parent.obj.get("verbose") if ctx.parent else False
        try:
            result = crawl_url(url, ignore_robots=ignore_robots)
            if result is None:
                click.echo(f"No content extracted from {url}", fg="yellow")
                sys.exit(1)
            title = result.get('title') or 'No title'
            link = result.get('link') or url
            click.echo(f"Crawled: {title} ({link})", fg="green")
            if verbose:
                content_preview = result.get('content', '')[:200] + '...' if result.get('content') else ''
                if content_preview:
                    click.echo(f"Content preview: {content_preview}")
        except Exception as e:
            click.echo(f"Error: Failed to crawl {url}: {e}", err=True, fg="red")
            logger.exception("Failed to crawl")
            sys.exit(1)

    Note: Ensure the import for crawl_url is added to the existing imports section (around line 14-22).
  </action>
  <verify>
    <automated>grep -n "@cli.command(\"crawl\")" /Users/y3/radar/src/cli.py && grep -n "from src.crawl import crawl_url" /Users/y3/radar/src/cli.py && python -m src.cli crawl --help | head -20</automated>
  </verify>
  <done>
    src/cli.py has:
    - Import: from src.crawl import crawl_url
    - Command: @cli.command("crawl") with @click.argument("url") and @click.option("--ignore-robots")
    - Success output: "Crawled: {title} ({link})" in green
    - Error output: "Error: Failed to crawl..." in red
    - No-content output: "No content extracted from..." in yellow
  </done>
</task>

</tasks>

<verification>
## Phase Verification

1. `python -m src.cli crawl --help` shows usage with --ignore-robots option
2. `python -m src.cli crawl --ignore-robots --help` shows the flag is recognized
3. Manual test: `python -m src.cli crawl https://example.com` (may fail due to network/content, but command structure works)

## Integration Check

After both plans complete:
1. `python -m src.cli article list | grep -i crawled` should show crawled articles
2. `python -m src.cli search "example"` should find crawled content
</verification>

<success_criteria>
- `crawl` command exists and accepts URL argument
- `--ignore-robots` flag is available
- Success message shows article title
- Error message shows reason in red
- Crawled articles appear in article list with feed name "Crawled Pages"
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-crawling/03-02-SUMMARY.md`
</output>
