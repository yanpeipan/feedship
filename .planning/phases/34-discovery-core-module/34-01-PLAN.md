---
phase: 34-discovery-core-module
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discovery/__init__.py
  - src/discovery/models.py
  - src/discovery/common_paths.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
must_haves:
  truths:
    - "discover_feeds(url) returns list of DiscoveredFeed objects"
    - "HTML <head> link tags with rel='alternate' are parsed"
    - "Well-known paths are probed when no autodiscovery tags found"
    - "Relative URLs in link href are resolved to absolute URLs"
    - "Discovered feeds are validated via HEAD request"
    - "Bozo feeds are identified and filtered"
  artifacts:
    - path: "src/discovery/models.py"
      provides: "DiscoveredFeed dataclass with url, title, feed_type, source, page_url"
      min_lines: 25
    - path: "src/discovery/common_paths.py"
      provides: "WELL_KNOWN_PATHS tuple for fallback probing"
      min_lines: 10
    - path: "src/discovery/__init__.py"
      provides: "discover_feeds() async entry point, public API exports"
      min_lines: 15
  key_links:
    - from: "src/discovery/__init__.py"
      to: "src/discovery/parser.py"
      via: "parse_link_elements()"
    - from: "src/discovery/__init__.py"
      to: "src/discovery/fetcher.py"
      via: "validate_feed()"
    - from: "src/discovery/parser.py"
      to: "src/discovery/models.py"
      via: "DiscoveredFeed dataclass"
---

<objective>
Create the discovery service package foundation with data models and constants.

Purpose: Establish the core data structures (DiscoveredFeed dataclass) and well-known path constants that all subsequent modules depend on.
Output: src/discovery/ package with models.py, common_paths.py, and empty __init__.py shell
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/y3/radar/CLAUDE.md
@/Users/y3/radar/src/models.py
@/Users/y3/radar/src/providers/rss_provider.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create discovery package with DiscoveredFeed model and well-known paths</name>
  <files>src/discovery/__init__.py, src/discovery/models.py, src/discovery/common_paths.py</files>
  <action>
Create src/discovery/ package directory with:

1. src/discovery/__init__.py - Empty shell with docstring:
   """Discovery service for RSS/Atom feed auto-discovery from website URLs."""
   exports: discover_feeds (when implemented)

2. src/discovery/models.py - DiscoveredFeed dataclass:
   ```python
   from dataclasses import dataclass
   from typing import Optional

   @dataclass
   class DiscoveredFeed:
       """Represents a discovered RSS/Atom/RDF feed from a website URL.

       Attributes:
           url: Absolute URL of the discovered feed.
           title: Title of the feed if available from autodiscovery link, else None.
           feed_type: Feed type string ('rss', 'atom', 'rdf') detected from type attribute.
           source: How the feed was discovered ('autodiscovery' or 'well_known_path').
           page_url: The original page URL where this feed was discovered.
       """
       url: str
       title: Optional[str]
       feed_type: str  # 'rss', 'atom', or 'rdf'
       source: str  # 'autodiscovery' or 'well_known_path'
       page_url: str  # Original page URL
   ```

3. src/discovery/common_paths.py - Well-known feed paths:
   ```python
   """Well-known feed URL paths for fallback probing (DISC-02)."""
   from typing import Sequence

   WELL_KNOWN_PATHS: tuple[str, ...] = (
       "/feed",
       "/feed/",
       "/rss",
       "/rss.xml",
       "/atom.xml",
       "/feed.xml",
       "/index.xml",
   )

   # MIME types for feed Content-Type validation (DISC-04)
   FEED_CONTENT_TYPES: tuple[str, ...] = (
       "application/rss+xml",
       "application/atom+xml",
       "application/rdf+xml",
       "application/xml",
       "text/xml",
   )
   ```

All files use `from __future__ import annotations` and follow existing codebase patterns.
</action>
  <verify>python -c "from src.discovery import DiscoveredFeed; from src.discovery.common_paths import WELL_KNOWN_PATHS; print('OK')"</verify>
  <done>src/discovery/ package created with DiscoveredFeed dataclass and WELL_KNOWN_PATHS constant</done>
</task>

</tasks>

<verification>
python -c "
from src.discovery.models import DiscoveredFeed
from src.discovery.common_paths import WELL_KNOWN_PATHS, FEED_CONTENT_TYPES
# Verify DiscoveredFeed has required fields
f = DiscoveredFeed(url='http://example.com/feed.xml', title='Example Feed', feed_type='rss', source='autodiscovery', page_url='http://example.com')
assert f.url == 'http://example.com/feed.xml'
assert f.feed_type == 'rss'
# Verify WELL_KNOWN_PATHS contains all required paths
assert '/feed' in WELL_KNOWN_PATHS
assert '/rss.xml' in WELL_KNOWN_PATHS
assert '/atom.xml' in WELL_KNOWN_PATHS
print('Verification passed')
"</verification>

<success_criteria>
1. DiscoveredFeed dataclass exists with url, title, feed_type, source, page_url fields
2. WELL_KNOWN_PATHS tuple contains all 7 required paths
3. FEED_CONTENT_TYPES tuple contains MIME types for feed validation
4. Package can be imported without errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-discovery-core-module/34-01-SUMMARY.md`
</output>
