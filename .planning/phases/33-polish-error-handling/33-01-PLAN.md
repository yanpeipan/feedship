---
phase: 33-polish-error-handling
plan: "33-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/vector.py
  - src/cli/article.py
autonomous: true
requirements_addressed: [SEM-07]
must_haves:
  truths:
    - "Articles fetched before v1.8 show friendly message when queried semantically"
    - "ChromaDB errors are caught and reported without crash"
    - "article related on article without embedding shows helpful message"
    - "search --semantic on empty index handles gracefully"
  artifacts:
    - path: "src/storage/vector.py"
      contains:
        - "def get_related_articles"
        - "no_embedding_article"
        - "chroma_error"
    - path: "src/cli/article.py"
      contains:
        - "article was fetched before v1.8"
        - "semantic search unavailable"
---

<objective>
Add graceful error handling for semantic search edge cases. All ChromaDB errors are caught and reported with user-friendly messages instead of tracebacks.
</objective>

<context>
@src/storage/vector.py
@src/cli/article.py
@src/storage/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add friendly error messages to vector.py functions</name>
  <read_first>
    src/storage/vector.py
    src/storage/__init__.py
  </read_first>
  <action>
    Modify `src/storage/vector.py` to handle errors gracefully:

    **1. Modify `get_related_articles()` (lines 190-253):**
    - Instead of `raise ValueError(f"No embedding found for article...")` at line 220
    - Change to: `logger.info("Article %s has no embedding (fetched before v1.8)", article_id)` then `return []`
    - This allows the CLI to show a friendly message

    **2. Wrap ChromaDB operations in try/except in `search_articles_semantic()`:**
    - Lines 162-170: The `collection.query()` call
    - On exception: `logger.warning("ChromaDB query failed: %s", e)` then `return []`
    - This prevents crash on corruption/disk full

    **3. Wrap ChromaDB operations in try/except in `get_related_articles()`:**
    - Lines 213-221: The `collection.get()` call
    - On exception: `logger.warning("ChromaDB get failed: %s", e)` then `return []`
    - Lines 224-232: The `collection.query()` call
    - On exception: `logger.warning("ChromaDB query failed: %s", e)` then `return []`

    **Key messages to use:**
    - For "no embedding": `logger.info("Article %s has no embedding (fetched before v1.8)", article_id)` before returning `[]`
    - For ChromaDB errors: `logger.warning("ChromaDB error in get_related_articles: %s", e)` before returning `[]`

    DO NOT change the function signatures or return types.
  </action>
  <verify>
    grep -r "has no embedding" src/storage/vector.py
    grep -r "ChromaDB error" src/storage/vector.py
  </verify>
  <done>
    get_related_articles() returns empty list (not raises) when article has no embedding.
    Both search functions return empty list (not raise) on ChromaDB errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLI commands to show friendly error messages</name>
  <read_first>
    src/cli/article.py
    src/storage/vector.py
  </read_first>
  <action>
    Modify `src/cli/article.py` to show friendly messages:

    **1. Modify `article_search()` command (lines 286-364):**
    - After line 307 where `search_articles_semantic()` is called:
    - Add check: if results is empty list AND no exception, don't show error
    - The current code already handles empty results gracefully at line 309
    - For ChromaDB errors: the generic except at line 361 catches, but message is too technical
    - Change line 362 from `f"Error: Failed to search articles: {e}"` to check error type:
      - If ChromaDB error: `f"Semantic search unavailable: {e}. Your articles are still stored and searchable by keyword."`

    **2. Modify `article_related()` command (lines 367-406):**
    - After line 379 where `get_related_articles()` is called:
    - Add check BEFORE the for loop: if results is empty AND article exists in DB, it means no embedding
    - To detect "no embedding" case: the storage layer now returns empty list (not raises)
    - Add logic:
      ```
      # Check if article exists but has no embedding
      from src.storage.sqlite import get_article
      article = get_article(article_id)
      if article and not results:
          click.secho(f"Article was fetched before v1.8 and has no semantic embedding. Fetch the article again to enable similarity search.", fg="yellow")
      ```
    - For ChromaDB errors (empty results from storage errors): show `f"Semantic search unavailable: {e}. Your articles are still stored."`

    **Key messages:**
    - "Article was fetched before v1.8 and has no semantic embedding. Fetch the article again to enable similarity search."
    - "Semantic search unavailable: [error]. Your articles are still stored and searchable by keyword."

    DO NOT change the function signatures.
  </action>
  <verify>
    grep -r "article was fetched before v1.8" src/cli/article.py
    grep -r "semantic search unavailable" src/cli/article.py
  </verify>
  <done>
    article search --semantic shows friendly message on ChromaDB errors.
    article related shows friendly message when article has no embedding.
  </done>
</task>

</tasks>

<verification>
1. Run `python -m src.cli search --semantic "test"` on empty DB - should show friendly message, not traceback
2. Run `python -m src.cli article related INVALIDID` - should show friendly message
3. Articles without embeddings (pre-v1.8) return empty results with friendly message
</verification>

<success_criteria>
1. `grep -r "article was fetched before v1.8" src/` finds the friendly message in article.py
2. `grep -r "semantic search unavailable" src/` finds the ChromaDB error message in article.py
3. `grep -r "has no embedding" src/storage/vector.py` finds the info log message
4. ChromaDB errors (corruption, disk full) return empty results instead of crashing
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-error-handling/33-01-SUMMARY.md`
</output>
