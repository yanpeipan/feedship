---
phase: 29-cli-integration-tests
plan: "29-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cli.py
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "feed add <url> creates a feed in the database and returns success message"
    - "feed list shows all subscribed feeds with article counts"
    - "feed remove <id> removes a feed from the database"
    - "article list shows articles in a rich table format"
    - "article view <prefix> shows article detail with content"
    - "article search <query> returns matching articles via FTS5"
    - "tag add <name> creates a tag and returns success message"
    - "tag list shows all tags with article counts"
    - "tag remove <name> removes a tag from the database"
    - "Duplicate feed URL returns exit code 1 with error message"
    - "Non-existent feed/article/tag returns exit code 1 with not found message"
  artifacts:
    - path: tests/test_cli.py
      provides: CLI integration tests for feed, article, tag commands
      min_lines: 200
      exports: TestFeedCommands, TestArticleCommands, TestTagCommands
  key_links:
    - from: tests/test_cli.py
      to: src.cli
      via: CliRunner.invoke()
      pattern: cli_runner.invoke.*cli
    - from: tests/test_cli.py
      to: src.storage.sqlite
      via: initialized_db fixture patches _DB_PATH
      pattern: initialized_db.*_DB_PATH
---

<objective>
Write integration tests for CLI commands using CliRunner. Create tests/test_cli.py covering feed, article, and tag command groups with both success and error cases.
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-cli-integration-tests/29-CONTEXT.md
@src/cli/__init__.py
@src/cli/feed.py
@src/cli/article.py
@src/cli/tag.py
@tests/conftest.py
@tests/test_storage.py
@tests/test_providers.py
@src/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_cli.py with feed command tests</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (does not exist yet - will be created)
    - src/cli/feed.py
    - tests/conftest.py
  </read_first>
  <action>
Create tests/test_cli.py with the following structure:

1. Import required modules:
```python
"""Integration tests for CLI commands using CliRunner."""
import pytest
from click.testing import CliRunner
from src.cli import cli
from src.storage.sqlite import init_db, add_feed, store_article
from src.models import Feed
```

2. Create TestFeedCommands class with tests:
- test_feed_add_success: Invoke `cli_runner.invoke(cli, ['feed', 'add', 'https://example.com/feed.xml'])`, assert exit_code == 0 and 'Added feed' in output
- test_feed_add_duplicate_returns_error: Add feed first, then invoke feed add again with same URL, assert exit_code == 1 and 'Error' in output
- test_feed_list_empty: Invoke feed list with no feeds, assert exit_code == 0 and 'No feeds subscribed' in output
- test_feed_list_with_feeds: Add feeds via storage, invoke feed list, assert feed names appear in output
- test_feed_remove_success: Add feed, invoke feed remove with feed_id, assert exit_code == 0 and 'Removed feed' in output
- test_feed_remove_not_found: Invoke feed remove with non-existent ID, assert exit_code == 1 and 'not found' in output.lower()

3. Use `cli_runner` fixture for CliRunner instance and `initialized_db` fixture for database.

4. Each test should invoke commands through CliRunner.invoke() which handles isolated_filesystem automatically.

5. Key implementation detail: The cli() in __init__.py calls init_db() on every command. The initialized_db fixture patches src.storage.sqlite._DB_PATH before init_db() is called by the CLI. This ensures test database is used.
</action>
  <acceptance_criteria>
    - File tests/test_cli.py exists with shebang and module docstring
    - TestFeedCommands class defined with 6 test methods
    - Each test uses cli_runner.invoke() pattern
    - test_feed_add_success asserts exit_code == 0 and 'Added feed' in output
    - test_feed_add_duplicate_returns_error asserts exit_code == 1
    - test_feed_list_empty asserts 'No feeds subscribed' in output
    - test_feed_remove_not_found asserts exit_code == 1 and 'not found' in output.lower()
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/y3/radar && python -m pytest tests/test_cli.py::TestFeedCommands -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>TestFeedCommands class with 6 tests covering feed add/list/remove commands and error cases</done>
</task>

<task type="auto">
  <name>Task 2: Add TestArticleCommands class to test_cli.py</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py
    - src/cli/article.py
    - tests/conftest.py
  </read_first>
  <action>
Append TestArticleCommands class to tests/test_cli.py:

1. Create TestArticleCommands class with tests:
- test_article_list_empty: Invoke article list with no articles, assert exit_code == 0 and 'No articles found' in output
- test_article_list_with_articles: Add feed and article via storage, invoke article list, assert article title appears in output
- test_article_view_success: Add feed and article via storage, invoke article view with article_id prefix, assert title appears in output and exit_code == 0
- test_article_view_not_found: Invoke article view with non-existent ID, assert exit_code == 1 and 'not found' in output.lower()
- test_article_search_found: Add feed and article with specific content, invoke article search with query, assert article title in output
- test_article_search_not_found: Invoke article search with no matches, assert exit_code == 0 and 'No articles found' in output
- test_article_tag_manual: Add feed and article, invoke article tag <article-id> <tag-name>, assert exit_code == 0 and 'Tagged article' in output

2. For article list, the output uses rich table so check for title substring in output.

3. For article view, the output uses Panel so check for title in output.

4. For article search, the output format is "Title | Source | Date" so check for title substring.

5. Use the same cli_runner and initialized_db fixtures.
</action>
  <acceptance_criteria>
    - TestArticleCommands class defined with 7 test methods
    - test_article_list_empty asserts 'No articles found' in output
    - test_article_view_success asserts exit_code == 0 and title in output
    - test_article_view_not_found asserts exit_code == 1 and 'not found'
    - test_article_search_found asserts title in output
    - test_article_tag_manual asserts 'Tagged article' in output
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/y3/radar && python -m pytest tests/test_cli.py::TestArticleCommands -v --tb=short 2>&1 | head -80</automated>
  </verify>
  <done>TestArticleCommands class with 7 tests covering article list/view/search/tag commands</done>
</task>

<task type="auto">
  <name>Task 3: Add TestTagCommands class to test_cli.py</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py
    - src/cli/tag.py
    - tests/conftest.py
  </read_first>
  <action>
Append TestTagCommands class to tests/test_cli.py:

1. Create TestTagCommands class with tests:
- test_tag_add_success: Invoke tag add with tag name, assert exit_code == 0 and 'Created tag' in output
- test_tag_add_duplicate_returns_error: Add tag first, then invoke tag add again with same name, assert exit_code == 1
- test_tag_list_empty: Invoke tag list with no tags, assert exit_code == 0 and 'No tags created' in output
- test_tag_list_with_tags: Add tags via storage, invoke tag list, assert tag names appear in output
- test_tag_remove_success: Add tag, invoke tag remove with tag name, assert exit_code == 0 and 'Removed tag' in output
- test_tag_remove_not_found: Invoke tag remove with non-existent tag, assert exit_code == 1 and 'not found' in output.lower()

2. Use the same cli_runner and initialized_db fixtures.

3. The tag commands use add_tag(), list_tags(), remove_tag() from src.storage.sqlite.
</action>
  <acceptance_criteria>
    - TestTagCommands class defined with 6 test methods
    - test_tag_add_success asserts exit_code == 0 and 'Created tag' in output
    - test_tag_add_duplicate_returns_error asserts exit_code == 1
    - test_tag_list_empty asserts 'No tags created' in output
    - test_tag_remove_not_found asserts exit_code == 1 and 'not found'
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/y3/radar && python -m pytest tests/test_cli.py::TestTagCommands -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>TestTagCommands class with 6 tests covering tag add/list/remove commands</done>
</task>

</tasks>

<verification>
- Run full test suite: `python -m pytest tests/test_cli.py -v --tb=short`
- Verify all 19 tests pass (6 feed + 7 article + 6 tag)
- Verify no test makes real HTTP calls (should use httpx_mock or no network)
- Verify all tests use initialized_db fixture for database isolation
</verification>

<success_criteria>
1. tests/test_cli.py exists with 3 test classes and 19 test methods total
2. All feed command tests pass (6 tests)
3. All article command tests pass (7 tests)
4. All tag command tests pass (6 tests)
5. Error cases tested: duplicate feed, not found for feed/article/tag
6. All tests use CliRunner.invoke() with cli_runner fixture
7. All tests use initialized_db fixture for database isolation
</success_criteria>

<output>
After completion, create `.planning/phases/29-cli-integration-tests/29-01-SUMMARY.md`
</output>
