---
phase: 28-storage-unit-tests
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - tests/test_storage.py
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Articles can be stored and retrieved via store_article() and list_articles()"
    - "Articles can be searched via search_articles() using FTS5"
    - "Feeds can be added, listed, retrieved, and removed via CRUD operations"
    - "Tags can be created, listed, and removed"
    - "Articles can be tagged and untagged, with tags retrievable per article"
  artifacts:
    - path: tests/test_storage.py
      provides: Unit tests for all storage layer public interfaces
      min_lines: 200
  key_links:
    - from: tests/test_storage.py::TestArticleOperations
      to: src/storage/sqlite.py
      via: import storage functions, call with real SQLite via initialized_db
    - from: tests/test_storage.py::TestFeedOperations
      to: src/storage/sqlite.py
      via: import storage functions, call with real SQLite via initialized_db
    - from: tests/test_storage.py::TestTagOperations
      to: src/storage/sqlite.py
      via: import storage functions, call with real SQLite via initialized_db
---

<objective>
Write comprehensive unit tests for SQLite storage layer functions covering Article operations, Feed CRUD, and Tag operations using real SQLite via tmp_path fixture.
</objective>

<context>
@/Users/y3/radar/src/storage/sqlite.py
@/Users/y3/radar/src/models.py
@/Users/y3/radar/src/application/articles.py
@/Users/y3/radar/tests/conftest.py
@/Users/y3/radar/tests/test_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Article storage tests</name>
  <files>tests/test_storage.py</files>
  <action>
Create tests/test_storage.py with TestArticleOperations class testing:

1. store_article() - stores article, returns article_id (nanoid format)
2. store_article_async() - async wrapper that serializes writes via asyncio.Lock
3. list_articles() - returns list of ArticleListItem ordered by pub_date DESC
4. list_articles() with feed_id filter - returns only articles from that feed
5. get_article() - returns single ArticleListItem by ID
6. get_article_detail() - returns dict with all fields including tags
7. get_article_detail() with truncated 8-char ID - matches prefix
8. search_articles() - FTS5 search returns matching articles
9. search_articles() with feed_id filter - FTS5 search within feed
10. search_articles() with empty query - returns empty list
11. list_articles_with_tags() - returns articles with specific tag
12. list_articles_with_tags() with tags (OR logic) - returns articles with any of the tags
13. get_articles_with_tags() - batch fetch tags for multiple article IDs

Each test must:
- Use `initialized_db` fixture (provides real SQLite database via tmp_path)
- First create a feed via add_feed() since articles require feed_id FK
- Use sample_feed fixture as template for feed data
- Assert on return types and field values

Pattern from test_providers.py - use class-based organization with descriptive test names.
  </action>
  <verify>
    <automated>pytest tests/test_storage.py::TestArticleOperations -v</automated>
  </verify>
  <done>TestArticleOperations class with 13+ test methods covering all article storage functions</done>
</task>

<task type="auto">
  <name>Task 2: Write Feed storage tests</name>
  <files>tests/test_storage.py</files>
  <action>
Add TestFeedOperations class to tests/test_storage.py testing:

1. feed_exists() with existing URL - returns True
2. feed_exists() with non-existing URL - returns False
3. add_feed() - inserts feed, returns Feed object
4. add_feed() duplicate URL - raises sqlite3.IntegrityError (UNIQUE constraint)
5. list_feeds() after adding feeds - returns list with articles_count attribute
6. list_feeds() empty - returns empty list
7. get_feed() with existing ID - returns Feed object
8. get_feed() with non-existing ID - returns None
9. remove_feed() with existing ID - returns True, feed deleted
10. remove_feed() with non-existing ID - returns False

Each test must:
- Use `initialized_db` fixture
- Use sample_feed fixture as template for feed data
- Assert on return types and field values
- Test cascade delete (removing feed removes its articles)
  </action>
  <verify>
    <automated>pytest tests/test_storage.py::TestFeedOperations -v</automated>
  </verify>
  <done>TestFeedOperations class with 10+ test methods covering all feed storage functions</done>
</task>

<task type="auto">
  <name>Task 3: Write Tag storage tests</name>
  <files>tests/test_storage.py</files>
  <action>
Add TestTagOperations class to tests/test_storage.py testing:

1. add_tag() - creates tag, returns Tag object with nanoid id
2. add_tag() duplicate name - raises sqlite3.IntegrityError (UNIQUE constraint)
3. list_tags() after adding tags - returns list ordered by name
4. list_tags() empty - returns empty list
5. remove_tag() with existing name - returns True, tag deleted
6. remove_tag() with non-existing name - returns False
7. remove_tag() cascade - removing tag unlinks from articles
8. get_tag_article_counts() - returns {tag_name: count} for all tags
9. tag_article() - links article to tag, creates tag if not exists, returns True
10. tag_article() duplicate link - returns True (no error, already linked)
11. untag_article() with linked tag - returns True, link removed
12. untag_article() with non-linked tag - returns False
13. get_article_tags() for article with tags - returns list of tag names
14. get_article_tags() for article without tags - returns empty list

Each test must:
- Use `initialized_db` fixture
- First create feed and article since tags link to articles
- Assert on return types and field values
  </action>
  <verify>
    <automated>pytest tests/test_storage.py::TestTagOperations -v</automated>
  </verify>
  <done>TestTagOperations class with 14+ test methods covering all tag storage functions</done>
</task>

</tasks>

<verification>
pytest tests/test_storage.py -v --tb=short
</verification>

<success_criteria>
1. tests/test_storage.py exists with 3 test classes (TestArticleOperations, TestFeedOperations, TestTagOperations)
2. TestArticleOperations covers: store_article, store_article_async, list_articles, get_article, get_article_detail, search_articles, list_articles_with_tags, get_articles_with_tags
3. TestFeedOperations covers: feed_exists, add_feed, list_feeds, get_feed, remove_feed
4. TestTagOperations covers: add_tag, list_tags, remove_tag, get_tag_article_counts, tag_article, untag_article, get_article_tags
5. All tests use initialized_db fixture (real SQLite via tmp_path, no sqlite3 mocking)
6. pytest tests/test_storage.py -v passes with 37+ passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/28-storage-unit-tests/28-01-SUMMARY.md`
</output>
