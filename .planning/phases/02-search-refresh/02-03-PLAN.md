---
phase: 02-search-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/cli.py
autonomous: true
requirements:
  - CLI-06
must_haves:
  truths:
    - "User can search articles by keyword via CLI"
    - "Search results display in same format as article list"
  artifacts:
    - path: "src/cli.py"
      provides: "article search subcommand with query argument"
      contains: "@cli.command(\"search\")"
      min_lines: 30
  key_links:
    - from: "src/cli.py"
      to: "src/articles.py"
      via: "search_articles() import and call"
      pattern: "search_articles"
---

<objective>
Add `article search` subcommand to CLI that searches articles using the FTS5-powered search_articles() function.

Purpose: Implements CLI-06 requirement for `article search <query>` command
Output: `article search <query>` command with --limit and --feed-id options
</objective>

<execution_context>
@/Users/y3/radar/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/cli.py
@src/articles.py
@.planning/phases/02-search-refresh/02-RESEARCH.md (lines 200-232, Click Search Subcommand pattern)

# CLI Pattern from 02-RESEARCH.md
```python
@cli.command("search")
@click.argument("query")
@click.option("--limit", default=20, help="Maximum number of results")
@click.option("--feed-id", default=None, help="Filter by feed ID")
@click.pass_context
def article_search(ctx: click.Context, query: str, limit: int, feed_id: Optional[str]) -> None:
    """Search articles by keyword using full-text search."""
    verbose = ctx.parent and ctx.parent.obj.get("verbose") if ctx.parent else False
    try:
        articles = search_articles(query=query, limit=limit, feed_id=feed_id)
        if not articles:
            click.echo("No articles found matching your search.")
            return

        click.echo("Title | Feed | Date")
        click.echo("-" * 80)

        for article in articles:
            title = article.title or "No title"
            feed_name = article.feed_name or "Unknown"
            pub_date = article.pub_date or "No date"
            click.echo(f"{title[:50]} | {feed_name[:20]} | {pub_date[:10]}")
    except Exception as e:
        click.echo(f"Error: Failed to search articles: {e}", err=True, fg="red")
        logger.exception("Failed to search articles")
        sys.exit(1)
```

# Key Decisions (D-05, D-07)
- D-05: Same format as `article list` command (title | feed | date columns)
- D-07: `article search` subcommand with --limit and --feed-id filter options
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add article search subcommand to cli.py</name>
  <files>src/cli.py</files>
  <read_first>
    - src/cli.py
  </read_first>
  <acceptance_criteria>
    - grep -n "@cli.command(\"search\")" src/cli.py shows search command decorator
    - grep -n "search_articles" src/cli.py imports and calls search function
    - grep -n "@click.argument(\"query\")" src/cli.py shows query argument
    - grep -n "@click.option(\"--limit\"" src/cli.py shows limit option
    - grep -n "@click.option(\"--feed-id\"" src/cli.py shows feed-id option
    - Output format matches article list: "Title | Feed | Date"
  </acceptance_criteria>
  <action>
    In src/cli.py:

    1. Add import for search_articles (add to existing import from src.articles):
    ```python
    from src.articles import list_articles, search_articles
    ```

    2. Add the search subcommand after the article_list function (after line 183, before line 185):
    ```python
    @cli.command("search")
    @click.argument("query")
    @click.option("--limit", default=20, help="Maximum number of results")
    @click.option("--feed-id", default=None, help="Filter by feed ID")
    @click.pass_context
    def article_search(ctx: click.Context, query: str, limit: int, feed_id: Optional[str]) -> None:
        """Search articles by keyword using full-text search.

        Supports FTS5 query syntax:
        - Multiple words default to AND (all must match)
        - Use quotes for exact phrase: "machine learning"
        - Use OR for either: python OR ruby
        """
        verbose = ctx.parent and ctx.parent.obj.get("verbose") if ctx.parent else False
        try:
            articles = search_articles(query=query, limit=limit, feed_id=feed_id)
            if not articles:
                click.echo("No articles found matching your search.")
                return

            click.echo("Title | Feed | Date")
            click.echo("-" * 80)

            for article in articles:
                title = article.title or "No title"
                feed_name = article.feed_name or "Unknown"
                pub_date = article.pub_date or "No date"

                if verbose:
                    click.echo(f"\nTitle: {title}")
                    click.echo(f"Feed: {feed_name}")
                    click.echo(f"Date: {pub_date}")
                    if article.link:
                        click.echo(f"Link: {article.link}")
                    if article.description:
                        desc_preview = article.description[:100] + "..." if len(article.description) > 100 else article.description
                        click.echo(f"Description: {desc_preview}")
                else:
                    click.echo(f"{title[:50]} | {feed_name[:20]} | {pub_date[:10]}")
        except Exception as e:
            click.echo(f"Error: Failed to search articles: {e}", err=True, fg="red")
            logger.exception("Failed to search articles")
            sys.exit(1)
    ```

    This follows D-05 (same format as article list) and D-07 (--limit and --feed-id options).
  </action>
  <verify>
    <automated>grep -n "@cli.command(\"search\")" src/cli.py && grep -n "search_articles" src/cli.py && grep -n "Title | Feed | Date" src/cli.py</automated>
  </verify>
  <done>article search command implemented with query argument, --limit and --feed-id options</done>
</task>

</tasks>

<verification>
- `article search "keyword"` command works via CLI
- Results display in same format as `article list` (Title | Feed | Date)
- --limit option controls result count
- --feed-id option filters by feed
</verification>

<success_criteria>
- CLI-06 requirement satisfied: `article search <query>` command exists
- Command supports --limit and --feed-id options
- Output format consistent with article list command
- Command handles empty results gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-refresh/02-03-SUMMARY.md`
</output>
