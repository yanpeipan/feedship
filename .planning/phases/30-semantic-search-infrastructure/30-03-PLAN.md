---
phase: 30-semantic-search-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
files_modified:
  - src/storage/vector.py
autonomous: true
requirements:
  - SEM-01
  - SEM-02
  - SEM-03

must_haves:
  truths:
    - "ChromaDB PersistentClient singleton is reused across imports"
    - "ChromaDB collection 'articles' is accessible"
    - "Embedding model can be loaded without network download"
  artifacts:
    - path: "src/storage/vector.py"
      provides: "ChromaDB infrastructure with embedding function"
      contains: "get_chroma_collection\|_chroma_client"
  key_links:
    - from: "src/cli/__init__.py"
      to: "src/storage/vector.py"
      via: "preload_embedding_model() call"
      pattern: "from src.storage.vector import"
---

<objective>
Verify ChromaDB infrastructure is correctly set up: singleton reuses client, collection is accessible, and model can be loaded without network download. This plan runs after Plans 01 and 02 complete.
</objective>

<context>
@src/storage/vector.py (final implementation to verify)
@src/storage/__init__.py (exports to verify)
@src/cli/__init__.py (startup integration to verify)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

The following functions should exist and be importable:
- from src.storage.vector import get_chroma_collection, preload_embedding_model
- from src.storage import get_chroma_collection
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify ChromaDB infrastructure works correctly</name>
  <files>src/storage/vector.py</files>
  <read_first>
    - src/storage/vector.py (implementation to verify)
    - src/storage/__init__.py (exports to verify)
    - src/cli/__init__.py (startup to verify)
  </read_first>
  <action>
    Verify the ChromaDB infrastructure is correctly set up by checking:

    1. File structure and exports:
       - src/storage/vector.py exists with ChromaDB imports
       - src/storage/__init__.py exports get_chroma_collection
       - src/cli/__init__.py calls preload_embedding_model() after init_db()

    2. Singleton pattern verification (via grep):
       - _chroma_client is module-level in vector.py
       - _get_chroma_client() implements lazy initialization
       - get_chroma_collection() uses the singleton

    3. Embedding function verification (via grep):
       - get_embedding_function() returns SentenceTransformer
       - Model name is "all-MiniLM-L6-v2"
       - get_or_create_collection() receives the embedding function

    4. Model pre-download verification (via grep):
       - preload_embedding_model() exists in vector.py
       - cli/__init__.py imports and calls it after init_db()

    5. Manual verification command (to be run by executor):
       python3 -c "
       import sys
       sys.path.insert(0, '.')
       from src.storage.vector import get_chroma_collection, preload_embedding_model, _get_embedding_function
       # Test that embedding function works
       ef = _get_embedding_function()
       test_vec = ef(['test text'])
       print(f'Embedding dimension: {test_vec.shape[1]}')
       # Test collection
       col = get_chroma_collection()
       print(f'Collection name: {col.name}')
       print('ChromaDB infrastructure OK')
       "

    This should print "Embedding dimension: 384" and "Collection name: articles"
  </action>
  <acceptance_criteria>
    - src/storage/vector.py exports get_chroma_collection, preload_embedding_model, _get_embedding_function
    - src/storage/__init__.py exports get_chroma_collection
    - src/cli/__init__.py calls preload_embedding_model after init_db
    - _get_embedding_function returns SentenceTransformer that produces 384-dim vectors
    - get_chroma_collection returns collection named "articles"
    - Python verification command prints "Embedding dimension: 384" and "Collection name: articles"
  </acceptance_criteria>
  <verify>
    grep -n "def get_chroma_collection\|def preload_embedding_model\|def _get_embedding_function\|def _get_chroma_client\|_chroma_client" src/storage/vector.py && python3 -c "
import sys
sys.path.insert(0, '.')
from src.storage.vector import get_chroma_collection, _get_embedding_function
ef = _get_embedding_function()
test_vec = ef(['test text'])
print(f'Embedding dimension: {test_vec.shape[1]}')
col = get_chroma_collection()
print(f'Collection name: {col.name}')
"
  </verify>
  <done>ChromaDB infrastructure verified: singleton works, collection accessible, embeddings are 384-dim</done>
</task>

</tasks>

<verification>
- get_chroma_collection() returns a ChromaDB collection named "articles"
- _get_embedding_function() produces 384-dimensional vectors
- Model can be loaded without network download (pre-cached)
- CLI startup properly initializes everything
</verification>

<success_criteria>
- ChromaDB PersistentClient singleton reuses client across imports
- Collection "articles" is accessible for add/query operations
- Embedding model produces 384-dim vectors via all-MiniLM-L6-v2
- All requirements SEM-01, SEM-02, SEM-03 verified
</success_criteria>

<output>
After completion, create `.planning/phases/30-semantic-search-infrastructure/30-03-SUMMARY.md`
</output>
