---
phase: 30-semantic-search-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/vector.py
  - src/storage/__init__.py
autonomous: true
requirements:
  - SEM-01

must_haves:
  truths:
    - "ChromaDB PersistentClient is a module-level singleton in src/storage/"
    - "ChromaDB data stored in ~/.local/share/rss-reader/chroma/"
    - "Collection named 'articles' accessible for add/query operations"
  artifacts:
    - path: "src/storage/vector.py"
      provides: "ChromaDB PersistentClient singleton with module-level initialization"
      min_lines: 30
    - path: "src/storage/__init__.py"
      provides: "ChromaDB function exports alongside existing storage API"
      contains: "get_chroma_collection"
  key_links:
    - from: "src/storage/vector.py"
      to: "ChromaDB client"
      via: "module-level singleton _get_chroma_client()"
      pattern: "_chroma_client.*=.*PersistentClient"
    - from: "src/storage/__init__.py"
      to: "src/storage/vector.py"
      via: "import from vector module"
      pattern: "from src.storage.vector import"
---

<objective>
Create ChromaDB client infrastructure with PersistentClient singleton in src/storage/vector.py, using platformdirs for cross-platform data directory (~/.local/share/rss-reader/chroma/). Export get_chroma_collection() for downstream use.
</objective>

<context>
@src/storage/sqlite.py (reference: get_db() singleton pattern, lines 74-89)
@src/storage/__init__.py (current exports to extend)
@src/models.py (Article dataclass for type hints)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/storage/sqlite.py (singleton pattern to replicate):
```python
@contextmanager
def get_db():
    """Context manager for database connections."""
    conn = _get_connection()
    try:
        yield conn
    finally:
        conn.close()
```

From src/models.py:
```python
@dataclass
class Article:
    id: str
    feed_id: str
    guid: str
    created_at: str
    title: Optional[str] = None
    link: Optional[str] = None
    pub_date: Optional[str] = None
    description: Optional[str] = None
    content: Optional[str] = None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/storage/vector.py with ChromaDB PersistentClient singleton</name>
  <files>src/storage/vector.py</files>
  <read_first>
    - src/storage/sqlite.py (to see the get_db() singleton pattern at lines 74-89)
    - src/models.py (for Article dataclass type hints)
  </read_first>
  <action>
    Create src/storage/vector.py with the following:

    1. Import chromadb and sentence_transformers modules
    2. Use platformdirs.user_data_dir(appname="rss-reader") + "/chroma" for storage directory
    3. Create module-level singleton _chroma_client variable (initialized to None)
    4. Create _get_chroma_client() function that:
       - Returns existing client if already initialized (lazy singleton)
       - Creates PersistentClient with the chroma directory path
       - Stores in module-level _chroma_client
       - Returns the client
    5. Create get_chroma_collection() function that:
       - Gets client via _get_chroma_client()
       - Gets or creates collection named "articles"
       - Collection should have metadata: article ID, content, title, url
       - Returns the collection
    6. Create _get_embedding_function() that returns sentence_transformers embedding function:
       - Uses SentenceTransformer("all-MiniLM-L6-v2")
       - This produces 384-dimensional vectors
       - Same model as used in src/tags/ai_tagging.py

    Per D-01: ChromaDB PersistentClient instantiated in src/storage/ as module-level singleton
    Per D-02: ChromaDB data stored in ~/.local/share/rss-reader/chroma/ using platformdirs
    Per D-06: ChromaDB collection named "articles" with article ID, content, title, url metadata
  </action>
  <acceptance_criteria>
    - File src/storage/vector.py exists
    - Contains "_chroma_client" module-level variable initialized to None
    - Contains "_get_chroma_client()" function that creates PersistentClient
    - Contains "get_chroma_collection()" function that returns collection named "articles"
    - Uses platformdirs.user_data_dir(appname="rss-reader") + "/chroma" for directory
    - Contains "_get_embedding_function()" that uses SentenceTransformer("all-MiniLM-L6-v2")
    - ChromaDB import is "import chromadb"
  </acceptance_criteria>
  <verify>
    grep -n "PersistentClient\|_chroma_client\|get_chroma_collection\|_get_embedding_function\|platformdirs" src/storage/vector.py
  </verify>
  <done>ChromaDB client singleton created in src/storage/vector.py, collection "articles" accessible via get_chroma_collection()</done>
</task>

<task type="auto">
  <name>Task 2: Export ChromaDB functions from src/storage/__init__.py</name>
  <files>src/storage/__init__.py</files>
  <read_first>
    - src/storage/__init__.py (current exports to extend)
  </read_first>
  <action>
    Add ChromaDB exports to src/storage/__init__.py:

    1. Add import line for vector module functions:
       from src.storage.vector import get_chroma_collection, _get_embedding_function

    2. Add these to the existing import list alongside the sqlite imports

    Per D-07: src/storage/__init__.py exports ChromaDB functions alongside existing storage API
  </action>
  <acceptance_criteria>
    - src/storage/__init__.py contains "from src.storage.vector import"
    - get_chroma_collection is exported
    - _get_embedding_function is exported (internal use by vector.py)
  </acceptance_criteria>
  <verify>
    grep -n "vector\|chroma\|get_chroma_collection" src/storage/__init__.py
  </verify>
  <done>ChromaDB functions exported from src/storage/__init__.py alongside existing storage API</done>
</task>

</tasks>

<verification>
- src/storage/vector.py exists with ChromaDB PersistentClient singleton
- src/storage/__init__.py exports get_chroma_collection
- Collection "articles" can be accessed via get_chroma_collection()
</verification>

<success_criteria>
- ChromaDB PersistentClient created as module-level singleton in src/storage/vector.py
- ChromaDB storage directory: ~/.local/share/rss-reader/chroma/
- Collection named "articles" accessible via get_chroma_collection()
- Functions exported from src/storage/__init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/30-semantic-search-infrastructure/30-01-SUMMARY.md`
</output>
